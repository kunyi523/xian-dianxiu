import { Component, useEffect, useRef, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { create } from "zustand";
//#region src/game/data.ts
var RARITY = [
	"凡人",
	"炼气",
	"筑基",
	"金丹",
	"元婴",
	"化神",
	"合体",
	"仙人"
];
var BUILDINGS = [
	{
		id: "hall",
		name: "开山香案",
		sprite: "hall",
		flavor: "空峰第一炷香。烟起之处，山才认你作主人。",
		perk: "香火：全山山息随等级提升",
		tiers: [
			"残案",
			"香火",
			"仙案"
		],
		baseCost: 15,
		costScale: 1.15,
		baseDps: .5,
		color: "#1c1914",
		projectile: "orb"
	},
	{
		id: "house",
		name: "听松庐",
		sprite: "house",
		flavor: "山麓入山口。灯一盏盏亮起，便有人替你守夜开光。",
		perk: "人潮：缓慢香火自燃，并多开云游槽",
		tiers: [
			"茅阶",
			"云阶",
			"万灯"
		],
		baseCost: 100,
		costScale: 1.14,
		baseDps: 3,
		color: "#3a3630",
		projectile: "spark"
	},
	{
		id: "sword",
		name: "葬剑崖",
		sprite: "sword",
		flavor: "右脊插满旧剑。剑醒之后，自行出鞘绕峰。",
		perk: "剑鸣：悬剑冢、镇峰残纹额外增幅",
		tiers: [
			"剑石",
			"剑冢",
			"万剑鸣"
		],
		baseCost: 1100,
		costScale: 1.13,
		baseDps: 31,
		color: "#2a2622",
		projectile: "sword"
	},
	{
		id: "array",
		name: "镇岳碑",
		sprite: "array",
		flavor: "山左残台。一笔一笔补回阵纹，峰才肯稳住。",
		perk: "阵眼：镇峰残纹山息随等级提升",
		tiers: [
			"残纹",
			"镇峰",
			"伐天纹"
		],
		baseCost: 12e3,
		costScale: 1.13,
		baseDps: 160,
		color: "#a63d32",
		projectile: "sword"
	},
	{
		id: "mine",
		name: "潜龙渊",
		sprite: "mine",
		flavor: "山腹脉眼。疏通之后，脉晶自己往外渗。",
		perk: "脉晶：挂机渗出灵矿",
		tiers: [
			"脉眼",
			"龙脉",
			"灵渊"
		],
		baseCost: 13e4,
		costScale: 1.12,
		baseDps: 720,
		color: "#4a453e",
		projectile: "orb"
	},
	{
		id: "tower",
		name: "雷音塔",
		sprite: "tower",
		flavor: "近峰木骨。雷纹一接，指尖开光带神雷。",
		perk: "神雷：强化指尖开光",
		tiers: [
			"木骨",
			"雷骨",
			"九霄"
		],
		baseCost: 14e5,
		costScale: 1.12,
		baseDps: 4400,
		color: "#1c1914",
		projectile: "bolt"
	},
	{
		id: "mirror",
		name: "观劫台",
		sprite: "mirror",
		flavor: "崖上旧镜正对劫云。擦亮了，仙果来得勤。",
		perk: "望劫：开光增强，仙果更频",
		tiers: [
			"铜镜",
			"望劫",
			"星河"
		],
		baseCost: 2e7,
		costScale: 1.12,
		baseDps: 21e3,
		color: "#6e2a24",
		projectile: "orb"
	},
	{
		id: "alchemy",
		name: "丹鼎阁",
		sprite: "alchemy",
		flavor: "瀑边土灶。炉烟一起，灵草便顺水滴下。",
		perk: "丹烟：挂机渗出灵草",
		tiers: [
			"土灶",
			"丹烟",
			"太乙"
		],
		baseCost: 33e7,
		costScale: 1.11,
		baseDps: 117e3,
		color: "#a63d32",
		projectile: "spark"
	}
];
var DISCIPLES = [
	{
		id: "ago",
		name: "阿狗",
		title: "外门杂役",
		rarity: 0,
		bonus: {
			kind: "dps",
			value: .012
		},
		flavor: "山门扫地的。据说扫着扫着扫出了大道。",
		sprite: "outer",
		portrait: "ago"
	},
	{
		id: "xiaohua",
		name: "小花",
		title: "药园童子",
		rarity: 0,
		bonus: {
			kind: "explore",
			value: .03
		},
		flavor: "认得三百种能吃的草，其中十二种真能吃。",
		sprite: "outer",
		portrait: "xiaohua"
	},
	{
		id: "linzhou",
		name: "林小舟",
		title: "外门弟子",
		rarity: 1,
		bonus: {
			kind: "click",
			value: .04
		},
		flavor: "每天打坐到腿麻，腿麻即是悟道。",
		sprite: "outer",
		portrait: "ago"
	},
	{
		id: "qinghe",
		name: "清荷",
		title: "记名弟子",
		rarity: 1,
		bonus: {
			kind: "dps",
			value: .03
		},
		flavor: "话很少，剑很快。",
		sprite: "outer",
		portrait: "qinghe"
	},
	{
		id: "suqing",
		name: "苏清寒",
		title: "香火执簿",
		rarity: 2,
		bonus: {
			kind: "jade",
			value: .08
		},
		flavor: "管香火簿的。把缘玉算成了气运。",
		sprite: "elder",
		portrait: "chiyun"
	},
	{
		id: "tiewei",
		name: "铁卫",
		title: "护山执事",
		rarity: 2,
		bonus: {
			kind: "dps",
			value: .05
		},
		flavor: "一块会走路的盾。",
		sprite: "elder",
		portrait: "tiewei"
	},
	{
		id: "mowuchen",
		name: "墨无尘",
		title: "峰脊剑客",
		rarity: 3,
		bonus: {
			kind: "sword",
			value: .12
		},
		flavor: "从悬剑冢拔走的不是剑，是一道未干的墨痕。金丹以上可御剑绕劫。",
		sprite: "elder",
		portrait: "jianjiu",
		aura: "sword"
	},
	{
		id: "chiyun",
		name: "赤云真人",
		title: "丹烟司炉",
		rarity: 3,
		bonus: {
			kind: "click",
			value: .1
		},
		flavor: "瀑侧丹灶的烟认得他。开光时指尖会带一点朱砂热。",
		sprite: "elder",
		portrait: "chiyun",
		aura: "seal"
	},
	{
		id: "yunjun",
		name: "岚织",
		title: "云阶守灯",
		rarity: 4,
		bonus: {
			kind: "dps",
			value: .12
		},
		flavor: "夜里把山麓灯一盏盏织进云里。人在茅舍，山息在灯上。",
		sprite: "sword",
		portrait: "xuantian",
		aura: "cloud"
	},
	{
		id: "luohua",
		name: "拾烬",
		title: "云游引路",
		rarity: 4,
		bonus: {
			kind: "explore",
			value: .18
		},
		flavor: "专捡别人丢掉的机缘。云游地图上总有他先踩过的脚印。",
		sprite: "outer",
		portrait: "xiaohua",
		aura: "dawn"
	},
	{
		id: "jianjiu",
		name: "剑九",
		title: "悬剑冢主",
		rarity: 5,
		bonus: {
			kind: "sword",
			value: .22
		},
		flavor: "第九剑从未出鞘。出鞘那天，劫云要先让路。",
		sprite: "sword",
		portrait: "jianjiu",
		aura: "sword"
	},
	{
		id: "mingyue",
		name: "窥劫",
		title: "望镜司夜",
		rarity: 5,
		bonus: {
			kind: "crit",
			value: .08
		},
		flavor: "望劫镜擦亮的那一眼，暴机便写进开光里。",
		sprite: "sword",
		portrait: "qinghe",
		aura: "void"
	},
	{
		id: "xuantian",
		name: "玄天老祖",
		title: "劫余守峰",
		rarity: 6,
		bonus: {
			kind: "dps",
			value: .22
		},
		flavor: "闭关三千年，出来发现空峰已落满印记——他只是继续守。",
		sprite: "immortal",
		portrait: "xuantian",
		aura: "void"
	},
	{
		id: "wangji",
		name: "卧烟散人",
		title: "闭关庐客",
		rarity: 6,
		bonus: {
			kind: "offline",
			value: .35
		},
		flavor: "睡着时山息也不停。离山代收，靠的是他那口懒气。",
		sprite: "immortal",
		portrait: "xuantian",
		aura: "cloud"
	},
	{
		id: "tiandao",
		name: "无字",
		title: "天道残印",
		rarity: 7,
		bonus: {
			kind: "dps",
			value: .4
		},
		flavor: "碑上不曾刻名。天道记得这枚印，空峰便认这缕人。",
		sprite: "immortal",
		portrait: "chuchen",
		aura: "void"
	},
	{
		id: "chuchen",
		name: "初尘",
		title: "第一炷香",
		rarity: 7,
		bonus: {
			kind: "click",
			value: .45
		},
		flavor: "第一缕香火凝成的人。开光一下，空峰亮一下。",
		sprite: "immortal",
		portrait: "chuchen",
		aura: "dawn"
	}
];
var GACHA_WEIGHTS = [
	46,
	26,
	14,
	8,
	4,
	1.4,
	.45,
	.15
];
var SKILLS = [
	{
		id: "breath",
		name: "山息诀",
		desc: "全山山息",
		max: 20,
		cost: 1,
		costScale: 1.45,
		kind: "dps",
		value: .08
	},
	{
		id: "mind",
		name: "开光指",
		desc: "指尖开光所得香火",
		max: 20,
		cost: 1,
		costScale: 1.45,
		kind: "click",
		value: .08
	},
	{
		id: "swordheart",
		name: "剑冢印",
		desc: "剑系印记增幅",
		max: 15,
		cost: 2,
		costScale: 1.5,
		kind: "sword",
		value: .1
	},
	{
		id: "luck",
		name: "香火运",
		desc: "缘玉与开光暴机",
		max: 12,
		cost: 2,
		costScale: 1.55,
		kind: "jade",
		value: .06
	},
	{
		id: "cave",
		name: "闭关庐",
		desc: "离山时山门代收",
		max: 12,
		cost: 2,
		costScale: 1.5,
		kind: "offline",
		value: .12
	},
	{
		id: "seed",
		name: "劫后余种",
		desc: "劫后归山起步香火",
		max: 8,
		cost: 3,
		costScale: 1.65,
		kind: "startQi",
		value: 1
	},
	{
		id: "senseDao",
		name: "听劫",
		desc: "劫余道果与双倍机缘",
		max: 10,
		cost: 2,
		costScale: 1.55,
		kind: "dao",
		value: .05
	}
];
var CLICK_UPGRADES = [
	{
		id: "sense",
		name: "指尖开光",
		desc: "每一次开光更沉",
		baseCost: 20,
		scale: 1.18
	},
	{
		id: "critC",
		name: "劫纹窥机",
		desc: "提高开光暴机",
		baseCost: 80,
		scale: 1.22,
		max: 20
	},
	{
		id: "critD",
		name: "灵犀一触",
		desc: "提高暴机所得",
		baseCost: 120,
		scale: 1.24,
		max: 16
	},
	{
		id: "auto",
		name: "山中无为",
		desc: "香火自燃。需指尖开光 5 级",
		baseCost: 400,
		scale: 1.28,
		max: 25,
		needSense: 5
	}
];
var CRAFTS = [
	{
		id: "talisman",
		name: "开光符",
		desc: "指尖开光增强",
		max: 10,
		herbs: 8,
		ore: 2,
		jade: 2,
		kind: "click",
		value: .06
	},
	{
		id: "swordblank",
		name: "剑胚",
		desc: "剑系印记增幅",
		max: 10,
		herbs: 4,
		ore: 10,
		jade: 3,
		kind: "sword",
		value: .08
	},
	{
		id: "pill",
		name: "山息丹",
		desc: "全山山息",
		max: 10,
		herbs: 12,
		ore: 4,
		jade: 2,
		kind: "dps",
		value: .07
	},
	{
		id: "mirrorbit",
		name: "镜屑",
		desc: "开光暴机",
		max: 8,
		herbs: 6,
		ore: 8,
		jade: 4,
		kind: "crit",
		value: .02
	}
];
var REALMS = [
	{
		id: "backhill",
		name: "后山石径",
		duration: 20,
		unlockLayer: 0,
		jade: [1, 2],
		herbs: [1, 3],
		ore: [0, 1],
		qi: 40
	},
	{
		id: "mist",
		name: "紫霞涧",
		duration: 45,
		unlockLayer: 2,
		jade: [2, 4],
		herbs: [3, 6],
		ore: [1, 2],
		qi: 180
	},
	{
		id: "swordtomb",
		name: "古剑沉谷",
		duration: 90,
		unlockLayer: 6,
		jade: [3, 6],
		herbs: [2, 5],
		ore: [3, 6],
		qi: 700
	},
	{
		id: "ascend",
		name: "望仙台",
		duration: 150,
		unlockLayer: 12,
		jade: [5, 10],
		herbs: [4, 8],
		ore: [4, 8],
		qi: 2400
	}
];
var ORE_PER_MIN_PER_LV = .36;
var HERB_PER_MIN_PER_LV = .3;
//#endregion
//#region src/game/ink.ts
var TIER_NAME = [
	"",
	"草创",
	"开山",
	"仙府",
	"仙宫"
];
var PAL = {
	skyTop: "#dcd7c6",
	skyMid: "#e4dfd0",
	skyBot: "#ece5d2",
	peakFar: "#b0a898",
	peakMid: "#928676",
	peakNear: "#6b6558",
	ink: "#232429",
	inkSoft: "#4a443c",
	inkFaint: "rgba(35,36,41,",
	water: "#cfc8b4",
	mist: "rgba(236,229,210,",
	stone: "#d8d0bd",
	stoneEdge: "#a89f8d",
	gold: "#d8c690",
	seal: "#412d26",
	paper: "#ece5d2",
	ghost: "#a89f8d",
	robeBlue: "#5c4a3a",
	robeWhite: "#ece5d2"
};
/** 建筑五阶段:0废墟 1雏形(破) 2成型 3宏伟 4仙宫(满级) */
function buildingTier(lv) {
	if (lv <= 0) return 0;
	if (lv < 10) return 1;
	if (lv < 25) return 2;
	if (lv < 40) return 3;
	return 4;
}
function nextTierAt(lv) {
	if (lv < 1) return 1;
	if (lv < 10) return 10;
	if (lv < 25) return 25;
	if (lv < 40) return 40;
	return null;
}
function tierName(id, tier) {
	if (tier <= 0) return "废墟";
	return BUILDINGS.find((b) => b.id === id)?.tiers[tier - 1] ?? TIER_NAME[tier] ?? "";
}
var inkAssetBase = "/";
function setInkAssetBase(b) {
	inkAssetBase = b.replace(/\/$/, "");
}
/** 取融合建筑图:sprites/fused/{id}_t{n}.png,建筑+水墨山石+雾已烘焙一体,直接绘制。 */
var fusedImgCache = /* @__PURE__ */ new Map();
var fusedImgMissing = /* @__PURE__ */ new Set();
function buildingFusedImg(id, tier) {
	const key = `fused/${id}_t${Math.min(tier, id === "hall" ? 2 : 3)}`;
	let img = fusedImgCache.get(key);
	if (!img) {
		img = new Image();
		img.src = `${inkAssetBase}/${key}.png`;
		img.onerror = () => {
			if (!fusedImgMissing.has(key)) {
				fusedImgMissing.add(key);
				console.warn(`[ink] 融合建筑图缺失:${key}.png`);
			}
		};
		fusedImgCache.set(key, img);
	}
	return img.complete && img.naturalWidth > 0 ? img : null;
}
var fxAssetBase = "/";
function setFxAssetBase(b) {
	fxAssetBase = b.replace(/\/$/, "");
}
var fxImgCache = /* @__PURE__ */ new Map();
var fxImgMissing = /* @__PURE__ */ new Set();
/** 取特效逐帧贴图,只拼 {kind}_f{n}.png(jielei_f1..f6 / xianqi_f1..f6)。缺图打日志,不做 fallback。 */
function fxImg(kind, frame) {
	const key = `${kind}_f${frame}`;
	let img = fxImgCache.get(key);
	if (!img) {
		img = new Image();
		img.src = `${fxAssetBase}/${key}.png`;
		img.onerror = () => {
			if (!fxImgMissing.has(key)) {
				fxImgMissing.add(key);
				console.warn(`[ink] 特效贴图缺失:${key}.png,不做 fallback`);
			}
		};
		fxImgCache.set(key, img);
	}
	return img.complete && img.naturalWidth > 0 ? img : null;
}
var craneFlyCache = [null, null];
var craneFlyLogged = false;
/** public/fx/crane_fly_1.png / crane_fly_2.png。缺图打一次日志，不回退。 */
function craneFlyImg(frame) {
	if (!craneFlyCache[frame]) {
		const img = new Image();
		img.src = `${fxAssetBase}/crane_fly_${frame + 1}.png`;
		img.onerror = () => {
			if (!craneFlyLogged) {
				craneFlyLogged = true;
				console.warn("[ink] 飞行仙鹤贴图缺失:crane_fly_1/2.png,不做 fallback");
			}
		};
		craneFlyCache[frame] = img;
	}
	const img = craneFlyCache[frame];
	return img && img.complete && img.naturalWidth > 0 ? img : null;
}
/**
* 双帧振翅仙鹤：按时间在上扬/下压两帧间切换，带飞行起伏与轻微倾摆。
* wPx 为期望翼展像素宽。
*/
function drawCraneFly(ctx, x, y, t, wPx, facing) {
	const img = craneFlyImg(Math.floor(t * 3) % 2 === 0 ? 0 : 1);
	if (!img) return;
	const bobY = Math.sin(t * 1.6) * wPx * .06;
	const tilt = Math.sin(t * 1.1) * .06;
	const ratio = img.naturalHeight / Math.max(1, img.naturalWidth);
	const dw = wPx;
	const dh = wPx * ratio;
	ctx.save();
	ctx.translate(x, y + bobY);
	ctx.rotate(tilt * facing);
	ctx.scale(facing, 1);
	ctx.drawImage(img, -dw / 2, -dh / 2, dw, dh);
	ctx.restore();
}
/**
* 精灵图统一 1600x1600,建筑脚底在底部 40px 边距处、水平居中。
* 绘制时全 tier 用同一逻辑尺寸,脚底锚定在 (x,y),换图不跳位。
*/
function drawSpriteBuilding(ctx, id, x, y, s, tier) {
	if (tier < 1) return;
	const img = buildingFusedImg(id, tier);
	if (!img) return;
	const size = s * 2;
	const feetFromTop = .9 * size;
	const dx = x - size / 2;
	const dy = y - feetFromTop;
	ctx.drawImage(img, dx, dy, size, size);
}
function perkLine(id, lv) {
	if (lv <= 0) return BUILDINGS.find((b) => b.id === id)?.perk ?? "";
	if (id === "hall") return `香火 全山山息 +${(lv * .5).toFixed(1)}%`;
	if (id === "house") return `人潮 香火自燃 ${(Math.floor(lv / 3) * .12).toFixed(2)}/秒`;
	if (id === "sword") return `剑鸣 剑系 +${(lv * 1.6).toFixed(0)}%`;
	if (id === "array") return `阵眼 劫层 +${(lv * 1.2).toFixed(0)}%`;
	if (id === "mine") return `脉晶 ${(lv * ORE_PER_MIN_PER_LV).toFixed(1)}/分`;
	if (id === "tower") return `神雷 开光 +${(lv * 1.2).toFixed(0)}%`;
	if (id === "mirror") return `望劫 开光 +${(lv * .8).toFixed(0)}%`;
	if (id === "alchemy") return `丹烟 ${(lv * HERB_PER_MIN_PER_LV).toFixed(1)}/分`;
	return "";
}
/** 世界宽度 = 屏幕宽 × WORLD_K,可左右滑动;中央(劫云/主建筑/瀑布)为开屏镜头 */
var WORLD_K = 4.4;
var BUILDING_SLOTS = Object.fromEntries([
	{
		id: "main",
		nx: .5,
		ny: .18,
		hw: .16,
		hh: .2,
		band: 0,
		z: 0
	},
	{
		id: "tower",
		nx: .859,
		ny: .168,
		hw: .045,
		hh: .045,
		band: 0,
		z: 1
	},
	{
		id: "mirror",
		nx: .83,
		ny: .577,
		hw: .045,
		hh: .045,
		band: 0,
		z: 2
	},
	{
		id: "alchemy",
		nx: .311,
		ny: .558,
		hw: .045,
		hh: .045,
		band: 1,
		z: 0
	},
	{
		id: "array",
		nx: .885,
		ny: .345,
		hw: .045,
		hh: .045,
		band: 0,
		z: 1
	},
	{
		id: "mine",
		nx: .774,
		ny: .793,
		hw: .045,
		hh: .045,
		band: 2,
		z: 0
	},
	{
		id: "house",
		nx: .303,
		ny: .828,
		hw: .045,
		hh: .045,
		band: 2,
		z: 0
	},
	{
		id: "hall",
		nx: .547,
		ny: .541,
		hw: .055,
		hh: .055,
		band: 1,
		z: 1
	},
	{
		id: "sword",
		nx: .626,
		ny: .893,
		hw: .045,
		hh: .045,
		band: 1,
		z: 2
	}
].filter((p) => p.id !== "main").map((p) => [p.id, {
	nx: p.nx,
	ny: p.ny,
	z: p.z,
	band: p.band,
	hw: p.hw,
	hh: p.hh
}]));
/** 拜山多径：世界坐标(nx 0-1 铺满 worldW);WORLD_K=4.4 母卷版 */
var MOUNTAIN_PATHS = [
	[
		{
			nx: .11,
			ny: .48
		},
		{
			nx: .18,
			ny: .36
		},
		{
			nx: .3,
			ny: .32
		},
		{
			nx: .42,
			ny: .36
		},
		{
			nx: .49,
			ny: .48
		},
		{
			nx: .47,
			ny: .62
		},
		{
			nx: .5,
			ny: .76
		},
		{
			nx: .47,
			ny: .88
		},
		{
			nx: .66,
			ny: .97
		},
		{
			nx: .7,
			ny: .9
		},
		{
			nx: .72,
			ny: .78
		},
		{
			nx: .66,
			ny: .66
		},
		{
			nx: .54,
			ny: .6
		},
		{
			nx: .28,
			ny: .6
		},
		{
			nx: .14,
			ny: .56
		},
		{
			nx: .11,
			ny: .48
		}
	],
	[
		{
			nx: .66,
			ny: .32
		},
		{
			nx: .753,
			ny: .38
		},
		{
			nx: .82,
			ny: .38
		},
		{
			nx: .857,
			ny: .4
		},
		{
			nx: .9,
			ny: .36
		},
		{
			nx: .86,
			ny: .46
		},
		{
			nx: .88,
			ny: .56
		},
		{
			nx: .82,
			ny: .7
		},
		{
			nx: .8,
			ny: .84
		},
		{
			nx: .7,
			ny: .72
		},
		{
			nx: .7,
			ny: .58
		},
		{
			nx: .66,
			ny: .44
		},
		{
			nx: .66,
			ny: .32
		}
	],
	[
		{
			nx: .619,
			ny: .33
		},
		{
			nx: .619,
			ny: .26
		},
		{
			nx: .619,
			ny: .18
		},
		{
			nx: .64,
			ny: .26
		},
		{
			nx: .67,
			ny: .36
		},
		{
			nx: .72,
			ny: .46
		},
		{
			nx: .619,
			ny: .33
		}
	]
];
MOUNTAIN_PATHS[0];
MOUNTAIN_PATHS.length;
var TRIB = {
	nx: .619,
	ny: .12
};
function slotXY(nx, ny, w, h, blit) {
	if (!blit) return {
		x: nx * w,
		y: ny * h
	};
	return {
		x: blit.dx + nx * blit.dw,
		y: blit.dy + ny * blit.dh
	};
}
function tribCenter(w, h, blit = null) {
	const p = slotXY(TRIB.nx, TRIB.ny, w, h, blit);
	return {
		cx: p.x,
		cy: p.y,
		R: Math.min(w, h) * .128
	};
}
function stampStates(buildings, layer = 0) {
	const out = [];
	let lockedShown = false;
	for (const def of BUILDINGS) {
		const lv = buildings[def.id] ?? 0;
		const idx = BUILDINGS.findIndex((b) => b.id === def.id);
		const prevOk = idx <= 0 || (buildings[BUILDINGS[idx - 1].id] ?? 0) > 0 || def.id === "alchemy" && ((buildings.mine ?? 0) > 0 || layer >= 8);
		if (lv > 0 || prevOk) out.push({
			id: def.id,
			owned: lv > 0,
			locked: false,
			lv
		});
		else if (!lockedShown) {
			out.push({
				id: def.id,
				owned: false,
				locked: true,
				lv: 0
			});
			lockedShown = true;
		}
	}
	return out;
}
function buildingsInBand(band) {
	return BUILDINGS.filter((b) => BUILDING_SLOTS[b.id]?.band === band).sort((a, b) => (BUILDING_SLOTS[a.id]?.z ?? 0) - (BUILDING_SLOTS[b.id]?.z ?? 0));
}
var SKIN = [
	"#e8c39a",
	"#dfb183",
	"#d09a6e",
	"#f0cfae",
	"#ddac82",
	"#cfa37e"
];
var HAIR = [
	"#232a2e",
	"#2e363b",
	"#1f2529",
	"#3a444a"
];
var ROBE = [
	"#ece5d2",
	"#7a3a2c",
	"#5c4a3a",
	"#f2ecdc",
	"#6b5a44",
	"#8c2f28"
];
var gradCache = /* @__PURE__ */ new Map();
function radialCached(ctx, key, x0, y0, r0, x1, y1, r1, stops) {
	let g = gradCache.get(key);
	if (!g) {
		g = ctx.createRadialGradient(x0, y0, r0, x1, y1, r1);
		for (const [o, c] of stops) g.addColorStop(o, c);
		if (gradCache.size > 60) gradCache.clear();
		gradCache.set(key, g);
	}
	return g;
}
var skyCache = /* @__PURE__ */ new Map();
function drawSky(ctx, w, h) {
	const key = Math.round(h);
	let g = skyCache.get(key);
	if (!g) {
		g = ctx.createLinearGradient(0, 0, 0, h);
		g.addColorStop(0, PAL.skyTop);
		g.addColorStop(.45, PAL.skyMid);
		g.addColorStop(1, PAL.skyBot);
		if (skyCache.size > 8) skyCache.clear();
		skyCache.set(key, g);
	}
	ctx.fillStyle = g;
	ctx.fillRect(0, 0, w, h);
}
function drawDistantPeaks(ctx, w, h) {
	ctx.fillStyle = PAL.peakFar;
	ctx.beginPath();
	ctx.moveTo(0, h * .38);
	ctx.lineTo(w * .3, h * .22);
	ctx.lineTo(w * .55, h * .32);
	ctx.lineTo(w, h * .18);
	ctx.lineTo(w, h);
	ctx.lineTo(0, h);
	ctx.fill();
}
function drawPeaks(ctx, w, h, band) {
	ctx.fillStyle = [
		PAL.peakFar,
		PAL.peakMid,
		PAL.peakNear
	][band];
	ctx.beginPath();
	const y0 = h * (.28 + band * .18);
	ctx.moveTo(0, h);
	ctx.lineTo(0, y0);
	ctx.quadraticCurveTo(w * .25, y0 - h * .08, w * .5, y0 + h * .04);
	ctx.quadraticCurveTo(w * .75, y0 - h * .06, w, y0);
	ctx.lineTo(w, h);
	ctx.fill();
}
function drawWaterfall(ctx, w, h, t) {
	ctx.save();
	ctx.globalAlpha = .5;
	ctx.strokeStyle = "#f4ecdc";
	ctx.lineWidth = 6;
	ctx.beginPath();
	ctx.moveTo(w * .18, h * .22);
	ctx.lineTo(w * .2, h * .62);
	ctx.stroke();
	ctx.globalAlpha = .35 + .12 * Math.sin(t * 4);
	ctx.lineWidth = 2;
	ctx.beginPath();
	ctx.moveTo(w * .175, h * .24 + t * 40 % 20);
	ctx.lineTo(w * .195, h * .6);
	ctx.stroke();
	ctx.restore();
}
function drawBandMist(ctx, w, h, band) {
	ctx.fillStyle = `${PAL.mist}${.12 + band * .05})`;
	ctx.fillRect(0, h * (.35 + band * .2), w, 28);
}
function drawForeground(ctx, w, h) {
	ctx.fillStyle = "#4a443c";
	ctx.beginPath();
	ctx.moveTo(0, h);
	ctx.lineTo(0, h * .92);
	ctx.quadraticCurveTo(w * .5, h * .88, w, h * .94);
	ctx.lineTo(w, h);
	ctx.fill();
}
function drawCultivator(ctx, x, y, variant, t, scale = 1, facing = 1) {
	const bob = Math.sin(t * 7.2) * 1.15;
	const stride = Math.sin(t * 8.4);
	const skin = SKIN[variant % SKIN.length];
	const hair = HAIR[variant % HAIR.length];
	const robe = ROBE[variant % ROBE.length];
	const female = variant % 3 === 1;
	ctx.save();
	ctx.translate(x, y + bob);
	ctx.scale(scale * facing, scale);
	ctx.fillStyle = hair;
	if (female) {
		ctx.fillRect(-2, -8, 5, 3);
		ctx.fillRect(-3, -6, 2, 5);
		ctx.fillRect(2, -6, 2, 5);
	} else {
		ctx.fillRect(-2, -8, 5, 3);
		ctx.fillRect(-1, -9, 3, 2);
	}
	ctx.fillStyle = skin;
	ctx.fillRect(-2, -6, 5, 4);
	ctx.fillStyle = PAL.ink;
	ctx.fillRect(-1, -5, 1, 1);
	ctx.fillRect(1, -5, 1, 1);
	ctx.fillStyle = robe;
	ctx.fillRect(-3, -2, 7, 5);
	ctx.fillStyle = variant % 2 === 0 ? PAL.seal : PAL.robeBlue;
	ctx.fillRect(-3, -1, 7, 1);
	ctx.fillStyle = robe;
	ctx.fillRect(-2 + stride * 1.2, 3, 2, 3.2);
	ctx.fillRect(1 - stride * 1.2, 3, 2, 3.2);
	ctx.fillStyle = PAL.ink;
	ctx.fillRect(-2 + stride * 1.2, 6, 2, 1);
	ctx.fillRect(1 - stride * 1.2, 6, 2, 1);
	ctx.restore();
}
function drawSwordRider(ctx, x, y, t, scale, facing, rarity) {
	const bob = Math.sin(t * 5.1) * 1.4;
	const tilt = Math.sin(t * 2.4) * .12;
	const robe = rarity >= 5 ? PAL.seal : rarity >= 3 ? PAL.ink : rarity >= 1 ? PAL.inkSoft : PAL.robeWhite;
	ctx.save();
	ctx.translate(x, y + bob);
	ctx.rotate(tilt);
	ctx.scale(scale * facing, scale);
	ctx.strokeStyle = PAL.ink;
	ctx.lineWidth = 1.4;
	ctx.lineCap = "round";
	ctx.beginPath();
	ctx.moveTo(-11, 5);
	ctx.lineTo(12, 2.4);
	ctx.stroke();
	ctx.fillStyle = "#e8c39a";
	ctx.fillRect(-2, -8, 4, 4);
	ctx.fillStyle = PAL.ink;
	ctx.fillRect(-2, -9, 4, 2);
	ctx.fillStyle = robe;
	ctx.fillRect(-3, -4, 6, 6);
	ctx.restore();
}
function drawTribCloud(ctx, cx, cy, s, pulse, t) {
	const pq = Math.round(pulse * 20) / 20;
	const sq = Math.round(s);
	const cxq = Math.round(cx);
	const cyq = Math.round(cy);
	const gk = `${cxq},${cyq},${sq},${pq}`;
	ctx.save();
	ctx.fillStyle = radialCached(ctx, `wash:${gk}`, cxq, cyq, sq * .15, cxq, cyq, sq * 2.4, [
		[0, `${PAL.inkFaint}${.16 + pq * .06})`],
		[.45, `${PAL.inkFaint}0.07)`],
		[1, `${PAL.inkFaint}0)`]
	]);
	ctx.beginPath();
	ctx.ellipse(cx, cy + s * .05, s * 2.35, s * 1.15, 0, 0, Math.PI * 2);
	ctx.fill();
	const fx = fxImg("jielei", Math.floor(t * 6) % 6 + 1);
	if (fx) {
		const fs = s * (4.7 + pulse * .18);
		ctx.globalAlpha = .97;
		ctx.drawImage(fx, cx - fs / 2, cy - fs / 2 + s * .05, fs, fs);
		ctx.globalAlpha = 1;
	}
	const sealR = sq * (.22 + pq * .12);
	ctx.fillStyle = radialCached(ctx, `seal:${gk}`, cxq + sq * .04, cyq - sq * .02, 0, cxq + sq * .04, cyq, sealR, [
		[0, `rgba(177,58,44,${.72 + pq * .2})`],
		[.45, `rgba(177,58,44,${.28 + pq * .12})`],
		[1, "rgba(177,58,44,0)"]
	]);
	ctx.beginPath();
	ctx.ellipse(cx + s * .05, cy, s * (.16 + pulse * .35), s * (.08 + pulse * .16), .12, 0, Math.PI * 2);
	ctx.fill();
	ctx.fillStyle = `rgba(236,229,210,${.22 + pulse * .15})`;
	ctx.beginPath();
	ctx.ellipse(cx - s * .06, cy - s * .05, s * .045, s * .022, -.4, 0, Math.PI * 2);
	ctx.fill();
	ctx.restore();
}
function drawQiThreads(ctx, x, y, t, n, spread, height, tint = "ink") {
	for (let i = 0; i < n; i++) {
		const seed = i * 1.618;
		const life = (t * .28 + seed) % 1;
		const sway = Math.sin(seed * 4 + t * .9) * spread * .35;
		const px = x + Math.sin(seed * 2.3) * spread * .45 + sway * life;
		const py = y - life * height;
		ctx.globalAlpha = (1 - life) * (1 - life) * .42;
		ctx.strokeStyle = tint === "seal" ? PAL.seal : tint === "paper" ? PAL.paper : PAL.inkSoft;
		ctx.lineWidth = 1 + (1 - life) * .6;
		ctx.beginPath();
		ctx.moveTo(x + Math.sin(seed) * spread * .25, y);
		ctx.quadraticCurveTo(x + sway, y - life * height * .55, px, py);
		ctx.stroke();
	}
	ctx.globalAlpha = 1;
}
/** 仙气六帧:建筑绘制之后的低透明 overlay,慢循环,不遮建筑主体。 */
function drawXianqiOverlay(ctx, x, y, s, t) {
	const img = fxImg("xianqi", Math.floor(t * 3) % 6 + 1);
	if (!img) return;
	const size = s * 2.2;
	ctx.save();
	ctx.globalAlpha = .22;
	ctx.drawImage(img, x - size / 2, y - 1.05 * s - size / 2, size, size);
	ctx.restore();
}
/** 未解锁槽位:淡色虚印地基。细虚线椭圆地基轮廓,低透明,不抢戏。 */
function drawGhostFoundation(ctx, x, y, s, t) {
	const breath = .2 + .07 * Math.sin(t * 1.15 + x * .01);
	ctx.save();
	ctx.strokeStyle = PAL.ghost;
	ctx.lineWidth = Math.max(1, s * .022);
	ctx.setLineDash([s * .085, s * .065]);
	ctx.globalAlpha = breath;
	ctx.beginPath();
	ctx.ellipse(x, y, s * .62, s * .2, 0, 0, Math.PI * 2);
	ctx.stroke();
	ctx.globalAlpha = breath * .65;
	ctx.beginPath();
	ctx.ellipse(x, y, s * .32, s * .105, 0, 0, Math.PI * 2);
	ctx.stroke();
	ctx.restore();
}
function drawSiteFx(ctx, x, y, lv, t, id, scale = 56, ruined = false, band = 2) {
	const tier = ruined ? 0 : buildingTier(lv);
	const s = Math.max(28, scale * [
		1,
		.85,
		1,
		1.15,
		1.3
	][tier]);
	const breath = .5 + .5 * Math.sin(t * 1.15 + x * .01);
	ctx.save();
	if (id) {
		if (tier < 1) {
			drawGhostFoundation(ctx, x, y, s, t);
			ctx.restore();
			return;
		}
		drawSpriteBuilding(ctx, id, x, y, s, tier);
		drawXianqiOverlay(ctx, x, y, s, t);
		ctx.restore();
		return;
	}
	if (id === "array") drawArraySite(ctx, x, y, s, tier, t, ruined, breath);
	else if (id === "sword") drawSwordSite(ctx, x, y, s, tier, t, ruined);
	else if (id === "tower") drawTowerSite(ctx, x, y, s, tier, t, ruined, breath);
	else if (id === "mine") drawMineSite(ctx, x, y, s, tier, t, ruined);
	else if (id === "alchemy") drawAlchemySite(ctx, x, y, s, tier, t, ruined, breath);
	else if (id === "mirror") drawMirrorSite(ctx, x, y, s, tier, t, ruined, breath);
	else if (id === "house") drawHouseSite(ctx, x, y, s, tier, t, ruined, breath);
	else if (id === "hall") drawHallSite(ctx, x, y, s, tier, t, ruined);
	ctx.restore();
}
function drawArraySite(ctx, x, y, s, tier, t, ruined, breath) {
	const pillars = [
		[
			0,
			-s * .38,
			ruined ? -.18 : 0
		],
		[
			-s * .46,
			s * .24,
			ruined ? .22 : 0
		],
		[
			s * .46,
			s * .24,
			ruined ? -.12 : 0
		]
	];
	for (const [px, py, rot] of pillars) {
		ctx.save();
		ctx.translate(x + px, y + py);
		ctx.rotate(rot);
		ctx.globalAlpha = ruined ? .32 : .65;
		ctx.strokeStyle = ruined ? "#5c564c" : "#2a2622";
		ctx.lineWidth = ruined ? 1.2 : 1.8;
		const h = ruined ? s * .14 : s * (.22 + tier * .03);
		ctx.beginPath();
		ctx.moveTo(0, 0);
		ctx.lineTo(0, -h);
		ctx.moveTo(-4.5, -h);
		ctx.lineTo(4.5, -h);
		ctx.stroke();
		ctx.restore();
	}
	ctx.strokeStyle = ruined ? "rgba(92,86,76,0.4)" : `rgba(166,61,50,${.38 + breath * .28})`;
	ctx.lineWidth = ruined ? 1 : 1.7;
	ctx.beginPath();
	ctx.moveTo(x, y - s * .38);
	ctx.lineTo(x + s * .46, y + s * .24);
	ctx.lineTo(x - s * .46, y + s * .24);
	ctx.closePath();
	ctx.stroke();
	if (tier >= 1) {
		ctx.beginPath();
		ctx.arc(x, y, s * .2, 0, Math.PI * 2);
		ctx.stroke();
	}
	if (tier >= 2) drawQiThreads(ctx, x, y, t, 5, s * .28, s * .7, "ink");
	if (tier >= 3) drawQiThreads(ctx, x, y - s * .1, t + 1, 4, s * .22, s * .85, "seal");
}
function drawSwordSite(ctx, x, y, s, tier, t, ruined) {
	const n = ruined ? 1 : 2 + tier;
	for (let i = 0; i < n; i++) {
		const ang = ruined ? -.6 : t * .7 + i * (Math.PI * 2 / n);
		const rr = ruined ? s * .18 : s * (.22 + .08 * Math.sin(t + i));
		ctx.save();
		ctx.globalAlpha = ruined ? .3 : .7;
		ctx.translate(x + Math.cos(ang) * rr, y + Math.sin(ang) * rr * .45 - 8);
		ctx.rotate(ang + .4);
		ctx.strokeStyle = ruined ? "#5c564c" : "#1c1914";
		ctx.lineWidth = 1.6;
		ctx.beginPath();
		ctx.moveTo(0, s * .16);
		ctx.lineTo(0, -s * .22);
		ctx.stroke();
		ctx.restore();
	}
	if (tier >= 2) drawQiThreads(ctx, x, y, t, 3 + tier, s * .2, s * .55, "ink");
}
function drawTowerSite(ctx, x, y, s, tier, t, ruined, breath) {
	if (ruined) {
		ctx.globalAlpha = .3;
		ctx.strokeStyle = "#5c564c";
		ctx.beginPath();
		ctx.moveTo(x - 6, y + 8);
		ctx.lineTo(x - 2, y - s * .4);
		ctx.stroke();
		return;
	}
	if (tier >= 1) drawQiThreads(ctx, x, y - s * .35, t, 3, s * .12, s * .55, "ink");
	if (tier >= 2) {
		ctx.strokeStyle = `rgba(28,25,20,${.4 + breath * .3})`;
		ctx.lineWidth = 1.4;
		ctx.beginPath();
		ctx.moveTo(x, y - s * .4);
		ctx.lineTo(x + Math.sin(t * 8) * s * .12, y - s * .95);
		ctx.stroke();
	}
}
function drawMineSite(ctx, x, y, s, tier, t, ruined) {
	ctx.globalAlpha = ruined ? .28 : .55;
	ctx.strokeStyle = "#2a2622";
	ctx.beginPath();
	ctx.moveTo(x - s * .38, y + s * .12);
	ctx.quadraticCurveTo(x, y - s * (ruined ? .12 : .28), x + s * .38, y + s * .12);
	ctx.stroke();
	if (!ruined && tier >= 2) drawQiThreads(ctx, x, y, t, 4, s * .22, s * .45, "ink");
}
function drawAlchemySite(ctx, x, y, s, tier, t, ruined, breath) {
	if (ruined) return;
	ctx.fillStyle = `rgba(166,61,50,${.32 + breath * .35})`;
	ctx.beginPath();
	ctx.moveTo(x - 4, y + s * .06);
	ctx.quadraticCurveTo(x, y - s * (.18 + breath * .1), x + 4, y + s * .06);
	ctx.fill();
	drawQiThreads(ctx, x, y - s * .05, t, 2 + tier, s * .12, s * .5, tier >= 3 ? "seal" : "ink");
}
function drawMirrorSite(ctx, x, y, s, tier, t, ruined, breath) {
	ctx.globalAlpha = ruined ? .25 : .55;
	ctx.strokeStyle = ruined ? "#5c564c" : "#2a2622";
	ctx.beginPath();
	ctx.ellipse(x, y - 2, s * .22, s * .3, 0, 0, Math.PI * 2);
	ctx.stroke();
	if (!ruined && tier >= 2) drawQiThreads(ctx, x, y - s * .1, t, 3, s * .16, s * .45, "paper");
}
function drawHouseSite(ctx, x, y, s, tier, t, ruined, breath) {
	if (ruined) return;
	ctx.fillStyle = `rgba(166,61,50,${.35 + breath * .4})`;
	ctx.fillRect(x - s * .28, y - 8, 4, 6);
	ctx.fillRect(x + s * .22, y - 6, 4, 6);
	if (tier >= 2) drawQiThreads(ctx, x, y, t, 3, s * .3, s * .35, "ink");
}
function drawHallSite(ctx, x, y, s, tier, t, ruined) {
	if (ruined) return;
	drawQiThreads(ctx, x, y + 4, t, 3 + tier, s * .22, s * .45, "ink");
	if (tier >= 3) drawQiThreads(ctx, x, y, t + 1.2, 3, s * .16, s * .55, "seal");
}
function drawUpgradeBurst(ctx, x, y, fl, tierUp) {
	if (fl <= 0) return;
	ctx.save();
	const a = Math.min(1, fl);
	ctx.globalAlpha = a * .55;
	ctx.fillStyle = PAL.paper;
	ctx.beginPath();
	ctx.ellipse(x, y + 6, 18 + (1 - a) * 70, 10 + (1 - a) * 28, 0, 0, Math.PI * 2);
	ctx.fill();
	ctx.globalAlpha = a * .7;
	ctx.fillStyle = tierUp ? "rgba(177,58,44,0.55)" : "rgba(236,229,210,0.55)";
	ctx.fillRect(x - (tierUp ? 5 : 3), y - (40 + (1 - a) * 90), tierUp ? 10 : 6, 50 + (1 - a) * 90);
	ctx.strokeStyle = `rgba(177,58,44,${a})`;
	ctx.lineWidth = tierUp ? 3 : 2;
	ctx.beginPath();
	ctx.arc(x, y, 16 + (1 - a) * (tierUp ? 70 : 40), 0, Math.PI * 2);
	ctx.stroke();
	ctx.restore();
}
//#endregion
//#region src/game/juice.ts
var fns = /* @__PURE__ */ new Set();
var juice = {
	on(fn) {
		fns.add(fn);
		return () => {
			fns.delete(fn);
		};
	},
	emit(e) {
		fns.forEach((f) => f(e));
	}
};
//#endregion
//#region src/game/sim.ts
var UNITS = [
	{
		v: 1e44,
		s: "载"
	},
	{
		v: 1e40,
		s: "正"
	},
	{
		v: 1e36,
		s: "涧"
	},
	{
		v: 1e32,
		s: "沟"
	},
	{
		v: 1e28,
		s: "穰"
	},
	{
		v: 1e24,
		s: "秭"
	},
	{
		v: 0x56bc75e2d63100000,
		s: "垓"
	},
	{
		v: 0x2386f26fc10000,
		s: "京"
	},
	{
		v: 0xe8d4a51000,
		s: "兆"
	},
	{
		v: 1e8,
		s: "亿"
	},
	{
		v: 1e4,
		s: "万"
	}
];
function formatNum(n) {
	if (!Number.isFinite(n)) return "∞";
	const sign = n < 0 ? "-" : "";
	const abs = Math.abs(n);
	if (abs < 1e3) return sign + (abs < 10 && abs % 1 !== 0 ? abs.toFixed(1) : Math.floor(abs).toString());
	for (const u of UNITS) if (abs >= u.v) {
		const x = abs / u.v;
		const digits = x >= 100 ? 0 : x >= 10 ? 1 : 2;
		return sign + x.toFixed(digits).replace(/\.0+$/, "") + u.s;
	}
	return sign + Math.floor(abs).toString();
}
function formatTime(sec) {
	sec = Math.max(0, Math.ceil(sec));
	const h = Math.floor(sec / 3600);
	const m = Math.floor(sec % 3600 / 60);
	const s = sec % 60;
	if (h > 0) return `${h}:${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")}`;
	return `${m}:${String(s).padStart(2, "0")}`;
}
function buildingCost(id, fromLevel, count = 1) {
	const b = BUILDINGS.find((x) => x.id === id);
	if (!b) return Infinity;
	const r = b.costScale;
	return b.baseCost * Math.pow(r, fromLevel) * ((Math.pow(r, count) - 1) / (r - 1));
}
function maxAffordable(id, fromLevel, qi) {
	if (qi <= 0) return 0;
	const b = BUILDINGS.find((x) => x.id === id);
	if (!b) return 0;
	const r = b.costScale;
	const first = b.baseCost * Math.pow(r, fromLevel);
	if (qi < first) return 0;
	const n = Math.floor(Math.log(qi * (r - 1) / first + 1) / Math.log(r));
	return Math.max(0, n);
}
function clickUpgradeCost(id, level) {
	const u = CLICK_UPGRADES.find((x) => x.id === id);
	if (!u) return Infinity;
	return u.baseCost * Math.pow(u.scale, level);
}
function skillCost(id, level) {
	const s = SKILLS.find((x) => x.id === id);
	if (!s) return Infinity;
	return Math.ceil(s.cost * Math.pow(s.costScale, level));
}
function layerHp(layer) {
	return 50 * Math.pow(1.62, layer);
}
function tribulationNeed(filled) {
	return 5e3 * Math.pow(1.55, filled);
}
function prestigeFruit(totalDamage, tribBonus) {
	if (totalDamage < 2e5) return 0;
	return Math.max(1, Math.floor(Math.sqrt(totalDamage / 2e5) * (1 + tribBonus)));
}
function sumBonus(state, kind) {
	let v = 0;
	for (const owned of state.disciples) {
		const def = DISCIPLES.find((d) => d.id === owned.id);
		if (!def || def.bonus.kind !== kind) continue;
		v += def.bonus.value * (1 + .25 * owned.stars);
	}
	for (const s of SKILLS) {
		if (s.kind !== kind) continue;
		v += s.value * (state.skills[s.id] ?? 0);
	}
	for (const c of CRAFTS) {
		if (c.kind !== kind) continue;
		v += c.value * (state.crafts[c.id] ?? 0);
	}
	return v;
}
function multipliers(state) {
	const prestige = 1 + .06 * state.prestigeCount + .3 * Math.sqrt(state.daoFruit);
	return {
		dps: (1 + sumBonus(state, "dps")) * prestige,
		click: 1 + sumBonus(state, "click"),
		jade: 1 + sumBonus(state, "jade"),
		crit: sumBonus(state, "crit"),
		explore: 1 + sumBonus(state, "explore"),
		sword: 1 + sumBonus(state, "sword"),
		offline: 1 + sumBonus(state, "offline"),
		dao: sumBonus(state, "dao")
	};
}
function clickPower(state) {
	const m = multipliers(state);
	const sense = 1 + (state.clickUp.sense ?? 0);
	const tower = 1 + .012 * (state.buildings.tower ?? 0);
	const mirror = 1 + .008 * (state.buildings.mirror ?? 0);
	const dpsShare = .01 * totalDps(state);
	return (sense * tower * mirror + dpsShare) * m.click;
}
function critChance(state) {
	const m = multipliers(state);
	return Math.min(.65, .04 + .03 * (state.clickUp.critC ?? 0) + m.crit);
}
function critMult(state) {
	return 2 + .25 * (state.clickUp.critD ?? 0);
}
function autoCps(state) {
	return .4 * (state.clickUp.auto ?? 0) + .12 * Math.floor((state.buildings.house ?? 0) / 3);
}
function buildingUnlocked(state, index) {
	if (index === 0) return true;
	const b = BUILDINGS[index];
	const prev = BUILDINGS[index - 1];
	if ((state.buildings[prev.id] ?? 0) >= 1) return true;
	if (b?.id === "alchemy") return (state.buildings.mine ?? 0) >= 1 || state.layer >= 8;
	return false;
}
function totalDps(state) {
	const m = multipliers(state);
	const hallMul = 1 + .005 * (state.buildings.hall ?? 0);
	const swordExtra = 1 + .016 * (state.buildings.sword ?? 0);
	const arrayMul = 1 + .012 * (state.buildings.array ?? 0);
	const layerMult = 1 + .008 * state.layer;
	let dps = 0;
	for (const b of BUILDINGS) {
		const lv = state.buildings[b.id] ?? 0;
		if (lv <= 0) continue;
		const sword = b.projectile === "sword" ? m.sword * swordExtra : 1;
		const layer = b.id === "array" ? arrayMul : 1;
		dps += b.baseDps * lv * m.dps * sword * hallMul * layer;
	}
	return dps * layerMult;
}
function gachaRoll(rareBonus) {
	const weights = GACHA_WEIGHTS.map((w, i) => i >= 3 ? w * (1 + rareBonus * 8) : w);
	const sum = weights.reduce((a, b) => a + b, 0);
	let r = Math.random() * sum;
	let rarity = 0;
	for (let i = 0; i < weights.length; i++) {
		r -= weights[i];
		if (r <= 0) {
			rarity = i;
			break;
		}
	}
	const pool = DISCIPLES.filter((d) => d.rarity === rarity);
	const def = pool[Math.floor(Math.random() * pool.length)] ?? DISCIPLES[0];
	return {
		def,
		rarity: def.rarity
	};
}
function exploreLoot(state, realmId) {
	const realm = REALMS.find((x) => x.id === realmId);
	if (!realm) return {
		jade: 0,
		herbs: 0,
		ore: 0,
		qi: 0
	};
	const m = multipliers(state).explore;
	const rng = (a, b) => a + Math.floor(Math.random() * (b - a + 1));
	return {
		jade: Math.round(rng(realm.jade[0], realm.jade[1]) * m),
		herbs: Math.round(rng(realm.herbs[0], realm.herbs[1]) * m),
		ore: Math.round(rng(realm.ore[0], realm.ore[1]) * m),
		qi: Math.round(realm.qi * m * (1 + .15 * state.layer))
	};
}
function startQiAfterPrestige(state) {
	const lv = state.skills.seed ?? 0;
	return 50 * Math.pow(8, lv);
}
//#endregion
//#region src/game/audio.ts
/** 水墨短合成：木鱼 / 磬 / 拨弦感，禁止锯齿电音主音色。尊重 setSfxEnabled。 */
var ctx = null;
var enabled = true;
var noiseBuf = null;
/** 总线：所有合成音先经 master 再出，静音可立即切断在播声音 */
var master = null;
function ac() {
	if (!enabled) return null;
	if (!ctx) {
		const C = window.AudioContext || window.webkitAudioContext;
		if (!C) return null;
		ctx = new C();
		master = ctx.createGain();
		master.gain.value = 1;
		master.connect(ctx.destination);
	}
	if (ctx.state === "suspended") ctx.resume();
	return ctx;
}
function setSfxEnabled(v) {
	enabled = v;
	if (ctx && master) master.gain.setTargetAtTime(v ? 1 : 0, ctx.currentTime, .02);
}
function noise(c) {
	if (noiseBuf && noiseBuf.sampleRate === c.sampleRate) return noiseBuf;
	const len = Math.floor(c.sampleRate * .35);
	const buf = c.createBuffer(1, len, c.sampleRate);
	const d = buf.getChannelData(0);
	for (let i = 0; i < len; i++) d[i] = (Math.random() * 2 - 1) * (1 - i / len) * .85;
	noiseBuf = buf;
	return buf;
}
/** 短振荡：三角/正弦为主，指数衰减 */
function tone(freq, dur, type, vol, at = 0, slideTo) {
	const c = ac();
	if (!c) return;
	const t0 = c.currentTime + at;
	const o = c.createOscillator();
	const g = c.createGain();
	o.type = type;
	o.frequency.setValueAtTime(freq, t0);
	if (slideTo != null) o.frequency.exponentialRampToValueAtTime(Math.max(20, slideTo), t0 + dur * .85);
	g.gain.setValueAtTime(1e-4, t0);
	g.gain.exponentialRampToValueAtTime(Math.max(2e-4, vol), t0 + .008);
	g.gain.exponentialRampToValueAtTime(1e-4, t0 + dur);
	o.connect(g);
	g.connect(master ?? c.destination);
	o.start(t0);
	o.stop(t0 + dur + .03);
}
/** 噪声脉冲：木鱼敲击 / 风噪余韵 */
function burst(dur, vol, at = 0, opts) {
	const c = ac();
	if (!c) return;
	const t0 = c.currentTime + at;
	const src = c.createBufferSource();
	src.buffer = noise(c);
	const g = c.createGain();
	g.gain.setValueAtTime(1e-4, t0);
	g.gain.exponentialRampToValueAtTime(Math.max(2e-4, vol), t0 + .004);
	g.gain.exponentialRampToValueAtTime(1e-4, t0 + dur);
	let node = src;
	if (opts?.band) {
		const bp = c.createBiquadFilter();
		bp.type = "bandpass";
		bp.frequency.value = opts.band;
		bp.Q.value = 1.4;
		node.connect(bp);
		node = bp;
	}
	if (opts?.hp) {
		const hp = c.createBiquadFilter();
		hp.type = "highpass";
		hp.frequency.value = opts.hp;
		node.connect(hp);
		node = hp;
	}
	if (opts?.lp) {
		const lp = c.createBiquadFilter();
		lp.type = "lowpass";
		lp.frequency.value = opts.lp;
		node.connect(lp);
		node = lp;
	}
	node.connect(g);
	g.connect(master ?? c.destination);
	src.start(t0);
	src.stop(t0 + dur + .02);
}
/** 磬/钟：正弦基音 + 弱泛音 + 短噪头 */
function bell(freq, dur, vol, at = 0) {
	tone(freq, dur, "sine", vol, at);
	tone(freq * 2.01, dur * .55, "sine", vol * .28, at + .01);
	tone(freq * 3.02, dur * .35, "triangle", vol * .12, at + .02);
	burst(Math.min(.05, dur * .15), vol * .35, at, {
		band: freq * 1.2,
		lp: freq * 4
	});
}
/** 拨弦感：三角主音微滑 + 噪声拨片 */
function pluck(freq, dur, vol, at = 0) {
	burst(.028, vol * .55, at, {
		hp: 800,
		lp: 4200,
		band: freq * 2
	});
	tone(freq, dur, "triangle", vol, at, freq * .97);
	tone(freq * 2, dur * .4, "sine", vol * .18, at + .008);
}
var sfx = {
	unlock() {
		ac();
	},
	/** 轻木鱼；暴击叠清越泛音 */
	click(crit) {
		burst(.055, crit ? .055 : .038, 0, {
			band: 380,
			lp: 1400,
			hp: 120
		});
		tone(180 + Math.random() * 40, .07, "sine", crit ? .045 : .028, 0);
		if (crit) {
			tone(920 + Math.random() * 60, .14, "sine", .032, .02);
			tone(1380, .1, "triangle", .018, .035);
		}
	},
	/** 上行五度拨弦 */
	upgrade() {
		pluck(392, .12, .038, 0);
		pluck(588, .14, .036, .07);
		pluck(784, .18, .032, .14);
	},
	/** 破层：低钟 + 余韵（必须走 ac→enabled） */
	layer() {
		bell(110, .42, .048, 0);
		tone(165, .35, "triangle", .028, .05);
		burst(.22, .03, .02, {
			lp: 600,
			hp: 40
		});
	},
	/** 印泥落印；高稀有多一声磬 */
	gacha(rarity) {
		burst(.06, .045, 0, {
			band: 220,
			lp: 900,
			hp: 80
		});
		pluck(440, .11, .032, .04);
		pluck(660, .12, .03, .11);
		if (rarity >= 3) bell(880, .28, .036, .18);
		if (rarity >= 5) bell(1320, .36, .03, .28);
	},
	/** 仙果：短琴泛音 */
	orb() {
		pluck(660, .14, .036, 0);
		tone(990, .18, "sine", .028, .05);
		tone(1320, .12, "sine", .016, .09);
	},
	/** 渡劫：钟磬长余韵 + 风噪，空间感大于破层 */
	prestige() {
		bell(98, .7, .05, 0);
		bell(147, .55, .035, .1);
		tone(220, .5, "triangle", .028, .18);
		burst(.55, .04, .05, {
			lp: 480,
			hp: 60
		});
		burst(.4, .022, .25, {
			lp: 320,
			hp: 40
		});
	}
};
//#endregion
//#region src/game/store.ts
var SAVE_KEY = "xian-clicker-v1";
var SAVE_VERSION = 1;
var toastSeq = 1;
function emptyBuildings() {
	const o = {};
	for (const b of BUILDINGS) o[b.id] = 0;
	return o;
}
function defaultState() {
	return {
		version: SAVE_VERSION,
		qi: 0,
		jade: 12,
		herbs: 0,
		ore: 0,
		daoFruit: 0,
		totalDamage: 0,
		lifetimeDamage: 0,
		layer: 0,
		layerHp: layerHp(0),
		tribulation: 0,
		tribFilled: 0,
		buildings: emptyBuildings(),
		clickUp: {
			sense: 0,
			critC: 0,
			critD: 0,
			auto: 0
		},
		disciples: [],
		skills: {},
		crafts: {},
		explores: [null],
		missions: starterMissions(),
		pity: 0,
		pulls: 0,
		clickCount: 0,
		layersBroken: 0,
		exploresDone: 0,
		prestigeCount: 0,
		labUnlocked: false,
		lastSave: Date.now(),
		started: false,
		seenTitle: false,
		tutorialStep: 0,
		combo: 0,
		lastClickAt: 0,
		autoAcc: 0,
		sfxOn: true,
		shakeOn: true
	};
}
function starterMissions() {
	return [
		{
			id: "m1",
			title: "开光劫云 20 次",
			kind: "clicks",
			target: 20,
			progress: 0,
			rewardJade: 6
		},
		{
			id: "m2",
			title: "点燃开山香案",
			kind: "building",
			target: 1,
			progress: 0,
			rewardJade: 8,
			buildingId: "hall"
		},
		{
			id: "m3",
			title: "揭开一层劫云",
			kind: "layers",
			target: 1,
			progress: 0,
			rewardJade: 8
		}
	];
}
function rollMission(state) {
	const templates = [
		{
			id: "",
			title: `开光 ${50 + state.prestigeCount * 20} 次`,
			kind: "clicks",
			target: 50 + state.prestigeCount * 20,
			progress: 0,
			rewardJade: 6
		},
		{
			id: "",
			title: "揭开 3 层劫云",
			kind: "layers",
			target: 3,
			progress: 0,
			rewardJade: 8
		},
		{
			id: "",
			title: "拜山投缘 1 次",
			kind: "pulls",
			target: 1,
			progress: 0,
			rewardJade: 5
		},
		{
			id: "",
			title: "完成云游 1 次",
			kind: "explore",
			target: 1,
			progress: 0,
			rewardJade: 6
		},
		{
			id: "",
			title: `开光累计 ${formatShort(2e3 * Math.pow(4, state.prestigeCount))} 山息`,
			kind: "damage",
			target: 2e3 * Math.pow(4, state.prestigeCount),
			progress: 0,
			rewardJade: 10
		}
	];
	const owned = BUILDINGS.filter((b, i) => i === 0 || (state.buildings[BUILDINGS[i - 1].id] ?? 0) > 0);
	const b = owned[Math.floor(Math.random() * owned.length)];
	const nextLv = (state.buildings[b.id] ?? 0) + 3 + Math.floor(Math.random() * 5);
	templates.push({
		id: "",
		title: `将${b.name}升至 ${nextLv} 级`,
		kind: "building",
		target: nextLv,
		progress: state.buildings[b.id] ?? 0,
		rewardJade: 8 + Math.floor(nextLv / 5),
		buildingId: b.id
	});
	if (!state.labUnlocked) templates.push({
		id: "",
		title: "开封藏经阁",
		kind: "lab",
		target: 1,
		progress: 0,
		rewardJade: 12
	});
	const m = templates[Math.floor(Math.random() * templates.length)];
	m.id = "m" + Math.random().toString(36).slice(2, 8);
	return m;
}
function formatShort(n) {
	if (n >= 1e8) return n / 1e8 + "亿";
	if (n >= 1e4) return n / 1e4 + "万";
	return String(n);
}
function applyDamage(s, amount) {
	s.qi += amount;
	s.totalDamage += amount;
	s.lifetimeDamage += amount;
	s.layerHp -= amount;
	s.tribulation += amount;
	let guard = 0;
	let broke = 0;
	while (s.layerHp <= 0 && guard++ < 24) {
		s.layer += 1;
		s.layersBroken += 1;
		s.layerHp += layerHp(s.layer);
		const jade = 1 + Math.floor(Math.random() * 3) + (Math.random() < multipliers(s).jade - 1 ? 1 : 0);
		s.jade += jade;
		if (Math.random() < .45) s.herbs += 1 + Math.floor(Math.random() * 2);
		if (Math.random() < .35) s.ore += 1;
		bumpMissions(s, "layers", 1);
		broke += 1;
	}
	if (broke > 0) {
		juice.emit({
			t: "layer",
			layer: s.layer
		});
		if (s.sfxOn) sfx.layer();
	}
	if (s.layerHp <= 0) s.layerHp = layerHp(s.layer);
	while (true) {
		const need = tribulationNeed(s.tribFilled);
		if (s.tribulation < need) break;
		s.tribulation -= need;
		s.tribFilled += 1;
		const gain = 1 + (Math.random() < multipliers(s).dao ? 1 : 0);
		s.daoFruit += gain;
	}
	bumpMissions(s, "damage", amount);
}
function bumpTutorial(s) {
	if (s.tutorialStep <= 0 || s.tutorialStep >= 5) return;
	let step = s.tutorialStep;
	if (step === 1 && s.clickCount >= 8) step = 2;
	if (step === 2 && (s.buildings.hall ?? 0) >= 1) step = 3;
	if (step === 3 && s.disciples.length >= 1) step = 4;
	s.tutorialStep = step;
}
function bumpMissions(s, kind, amount, buildingId) {
	for (const m of s.missions) {
		if (m.kind !== kind) continue;
		if (m.kind === "building") {
			if (buildingId && m.buildingId === buildingId) m.progress = s.buildings[buildingId] ?? 0;
			continue;
		}
		if (m.kind === "lab") {
			m.progress = s.labUnlocked ? 1 : 0;
			continue;
		}
		m.progress = Math.min(m.target, m.progress + amount);
	}
}
function persistNow(s) {
	try {
		const base = defaultState();
		const blob = {};
		for (const k of Object.keys(base)) blob[k] = s[k];
		blob.lastSave = Date.now();
		blob.combo = 0;
		blob.autoAcc = 0;
		localStorage.setItem(SAVE_KEY, JSON.stringify(blob));
		localStorage.setItem(SAVE_KEY + ":bak", JSON.stringify(blob));
	} catch {}
}
var saveTimer = 0;
var hudTimer = 0;
var lastFlushSig = /* @__PURE__ */ new Map();
function sig(v) {
	if (v === null || typeof v !== "object") return String(v);
	try {
		return JSON.stringify(v);
	} catch {
		return String(v);
	}
}
function flush(set, s) {
	const full = {
		qi: s.qi,
		jade: s.jade,
		herbs: s.herbs,
		ore: s.ore,
		daoFruit: s.daoFruit,
		totalDamage: s.totalDamage,
		lifetimeDamage: s.lifetimeDamage,
		layer: s.layer,
		layerHp: s.layerHp,
		tribulation: s.tribulation,
		tribFilled: s.tribFilled,
		buildings: { ...s.buildings },
		clickUp: { ...s.clickUp },
		disciples: s.disciples.map((d) => ({ ...d })),
		skills: { ...s.skills },
		crafts: { ...s.crafts },
		explores: s.explores.slice(),
		missions: s.missions.map((m) => ({ ...m })),
		pity: s.pity,
		pulls: s.pulls,
		clickCount: s.clickCount,
		layersBroken: s.layersBroken,
		exploresDone: s.exploresDone,
		prestigeCount: s.prestigeCount,
		labUnlocked: s.labUnlocked,
		lastSave: s.lastSave,
		started: s.started,
		seenTitle: s.seenTitle,
		tutorialStep: s.tutorialStep,
		combo: s.combo,
		lastClickAt: s.lastClickAt,
		autoAcc: s.autoAcc,
		sfxOn: s.sfxOn,
		shakeOn: s.shakeOn
	};
	const partial = {};
	let any = false;
	for (const k of Object.keys(full)) {
		const sg = sig(full[k]);
		if (lastFlushSig.get(k) !== sg) {
			lastFlushSig.set(k, sg);
			partial[k] = full[k];
			any = true;
		}
	}
	if (any) set(partial);
}
var useGame = create((set, get) => ({
	...defaultState(),
	tab: null,
	buyMode: 1,
	focusBuilding: null,
	gachaReveal: null,
	toasts: [],
	offlineGift: null,
	menuOpen: false,
	debug: typeof localStorage !== "undefined" && (() => {
		try {
			return localStorage.getItem("xian-debug") === "1";
		} catch {
			return false;
		}
	})(),
	hydrate() {
		try {
			const tryParse = (key) => {
				try {
					const raw = localStorage.getItem(key);
					return raw ? JSON.parse(raw) : null;
				} catch {
					return null;
				}
			};
			const loaded = tryParse(SAVE_KEY) ?? tryParse(SAVE_KEY + ":bak");
			const base = defaultState();
			const next = {
				...base,
				...loaded,
				version: SAVE_VERSION,
				buildings: {
					...base.buildings,
					...loaded?.buildings
				},
				clickUp: {
					...base.clickUp,
					...loaded?.clickUp
				},
				skills: {
					...base.skills,
					...loaded?.skills
				},
				crafts: {
					...base.crafts,
					...loaded?.crafts
				}
			};
			if (!next.missions?.length) next.missions = starterMissions();
			else for (const m of next.missions) {
				if (m.kind === "building" && m.buildingId) m.progress = Math.min(m.target, next.buildings[m.buildingId] ?? 0);
				if (m.kind === "lab") m.progress = next.labUnlocked ? 1 : 0;
			}
			if (!next.explores?.length) next.explores = [null];
			if (typeof next.tutorialStep !== "number" || next.tutorialStep < 0 || next.tutorialStep > 5) next.tutorialStep = next.started ? 5 : 0;
			const elapsed = Math.max(0, Date.now() - (next.lastSave || Date.now()));
			const dt = Math.min(elapsed, 288e5) / 1e3;
			let gift = null;
			if (dt > 15 && next.started) {
				const qi = totalDps(next) * dt * multipliers(next).offline * .5;
				next.qi += qi;
				next.lifetimeDamage += qi;
				gift = {
					sec: dt,
					qi
				};
			}
			sfx.unlock();
			setSfxEnabled(next.sfxOn !== false);
			const live = get();
			set({
				...next,
				started: live.started || next.started,
				seenTitle: live.seenTitle || next.seenTitle,
				tutorialStep: live.started ? live.tutorialStep : next.tutorialStep,
				tab: null,
				buyMode: 1,
				focusBuilding: null,
				gachaReveal: null,
				toasts: [],
				menuOpen: false,
				offlineGift: live.started ? null : gift,
				lastSave: Date.now()
			});
		} catch {
			set({
				...defaultState(),
				tab: null,
				buyMode: 1,
				focusBuilding: null,
				gachaReveal: null,
				toasts: [],
				menuOpen: false,
				offlineGift: null
			});
		}
	},
	persist() {
		persistNow(get());
	},
	setTab(t) {
		if (get().tab === t) set({
			tab: null,
			focusBuilding: null
		});
		else set({
			tab: t,
			focusBuilding: t === "sect" ? get().focusBuilding : null
		});
	},
	openSect(id) {
		set({
			tab: "sect",
			focusBuilding: id
		});
	},
	setBuyMode(m) {
		set({ buyMode: m });
	},
	startGame() {
		set({
			started: true,
			seenTitle: true,
			tutorialStep: get().tutorialStep === 0 ? 1 : get().tutorialStep,
			menuOpen: false,
			tab: null,
			gachaReveal: null
		});
		sfx.unlock();
		persistNow(get());
	},
	skipTutorial() {
		set({
			tutorialStep: 5,
			focusBuilding: null
		});
		persistNow(get());
	},
	finishTutorial() {
		set({
			tutorialStep: 5,
			focusBuilding: null
		});
		persistNow(get());
	},
	setMenu(v) {
		set({
			menuOpen: v,
			tab: v ? null : get().tab
		});
	},
	dismissOffline() {
		set({ offlineGift: null });
	},
	click(nx, ny, auto = false) {
		const s = get();
		if (!s.started) return;
		const now = performance.now();
		let combo = s.combo;
		if (!auto) combo = now - s.lastClickAt < 380 ? Math.min(24, combo + 1) : 1;
		const power = clickPower(s);
		const crit = Math.random() < critChance(s);
		const amount = power * (1 + combo * .04) * (crit ? critMult(s) : 1);
		s.combo = combo;
		s.lastClickAt = auto ? s.lastClickAt : now;
		s.clickCount = s.clickCount + (auto ? 0 : 1);
		applyDamage(s, amount);
		if (!auto) bumpMissions(s, "clicks", 1);
		bumpTutorial(s);
		if (s.tutorialStep === 2) set({ focusBuilding: "hall" });
		juice.emit({
			t: "click",
			x: nx,
			y: ny,
			n: amount,
			crit
		});
		if (s.sfxOn && (!auto || crit)) sfx.click(crit);
	},
	collectOrb(nx, ny) {
		const s = get();
		const m = multipliers(s);
		const amount = Math.max(clickPower(s) * 40, totalDps(s) * 8, 25);
		applyDamage(s, amount);
		s.jade += Math.random() < .5 * m.jade ? 1 : 0;
		flush(set, s);
		juice.emit({ t: "orb" });
		juice.emit({
			t: "click",
			x: nx,
			y: ny,
			n: amount,
			crit: true
		});
		if (s.sfxOn) sfx.orb();
		get().pushToast(`仙果 · 香火 +${Math.floor(amount)}`);
	},
	tick(dt) {
		const s = get();
		if (!s.started) return;
		const capped = Math.min(dt, .1);
		const dps = totalDps(s);
		if (dps > 0) applyDamage(s, dps * capped);
		const mineLv = s.buildings.mine ?? 0;
		if (mineLv > 0) s.ore += mineLv * ORE_PER_MIN_PER_LV * capped / 60;
		const alchLv = s.buildings.alchemy ?? 0;
		if (alchLv > 0) s.herbs += alchLv * HERB_PER_MIN_PER_LV * capped / 60;
		const cps = autoCps(s);
		if (cps > 0) {
			s.autoAcc += cps * capped;
			while (s.autoAcc >= 1) {
				s.autoAcc -= 1;
				get().click(.5 + (Math.random() - .5) * .06, .42 + (Math.random() - .5) * .05, true);
			}
		}
		if (s.combo > 0 && performance.now() - s.lastClickAt > 700) s.combo = 0;
		const slots = 1 + ((s.buildings.house ?? 0) >= 3 ? 1 : 0) + (mineLv >= 1 ? 1 : 0);
		while (s.explores.length < slots) s.explores.push(null);
		for (const m of s.missions) if (m.kind === "building" && m.buildingId) m.progress = Math.min(m.target, s.buildings[m.buildingId] ?? 0);
		hudTimer += capped;
		if (hudTimer > .12) {
			hudTimer = 0;
			flush(set, s);
		}
		saveTimer += capped;
		if (saveTimer > 4) {
			saveTimer = 0;
			persistNow(s);
		}
	},
	buyBuilding(id) {
		const s = get();
		const idx = BUILDINGS.findIndex((b) => b.id === id);
		if (idx < 0 || !buildingUnlocked(s, idx)) return;
		const lv = s.buildings[id] ?? 0;
		const mode = get().buyMode;
		const n = mode === "max" ? Math.max(1, maxAffordable(id, lv, s.qi)) : mode;
		const cost = buildingCost(id, lv, n);
		if (s.qi < cost || n < 1) return;
		s.qi -= cost;
		s.buildings[id] = lv + n;
		bumpMissions(s, "building", n, id);
		bumpTutorial(s);
		flush(set, s);
		persistNow(s);
		const nextT = buildingTier(lv + n);
		const prevT = buildingTier(lv);
		const def = BUILDINGS.find((b) => b.id === id);
		juice.emit({
			t: "upgrade",
			id,
			tierUp: nextT > prevT,
			name: def?.name
		});
		if (s.sfxOn) sfx.upgrade();
		if (nextT > prevT) get().pushToast(`${def?.name ?? ""} 进阶 · ${tierName(id, nextT)}`);
	},
	buyClick(id) {
		const s = get();
		const def = CLICK_UPGRADES.find((u) => u.id === id);
		if (!def) return;
		const lv = s.clickUp[id] ?? 0;
		if ("max" in def && def.max && lv >= def.max) return;
		if ("needSense" in def && def.needSense && (s.clickUp.sense ?? 0) < def.needSense) return;
		const cost = clickUpgradeCost(id, lv);
		if (s.qi < cost) return;
		s.qi -= cost;
		s.clickUp[id] = lv + 1;
		flush(set, s);
		juice.emit({ t: "upgrade" });
		if (s.sfxOn) sfx.upgrade();
	},
	pull(count) {
		const s = get();
		const price = count === 10 ? 90 : 10 * count;
		if (s.jade < price) return;
		s.jade -= price;
		const rareBonus = Math.max(0, multipliers(s).jade - 1);
		const reveal = [];
		for (let i = 0; i < count; i++) {
			let rolled = gachaRoll(rareBonus);
			s.pity += 1;
			if (s.pity >= 40 && rolled.rarity < 3) {
				const pool = DISCIPLES.filter((d) => d.rarity >= 3);
				const def = pool[Math.floor(Math.random() * pool.length)] ?? DISCIPLES[0];
				rolled = {
					def,
					rarity: def.rarity
				};
			}
			if (rolled.rarity >= 3) s.pity = 0;
			const existing = s.disciples.find((d) => d.id === rolled.def.id);
			const isNew = !existing;
			if (existing) existing.stars = Math.min(5, existing.stars + 1);
			else s.disciples.push({
				id: rolled.def.id,
				stars: 0
			});
			reveal.push({
				id: rolled.def.id,
				rarity: rolled.rarity,
				isNew
			});
			s.pulls += 1;
			bumpMissions(s, "pulls", 1);
			juice.emit({
				t: "gacha",
				rarity: rolled.rarity,
				name: rolled.def.name,
				id: rolled.def.id,
				isNew
			});
		}
		bumpTutorial(s);
		flush(set, s);
		set({ gachaReveal: reveal });
		persistNow(get());
		if (s.sfxOn) sfx.gacha(Math.max(...reveal.map((r) => r.rarity)));
		const best = reveal.reduce((a, b) => b.rarity > a.rarity ? b : a, reveal[0]);
		if (best) {
			const def = DISCIPLES.find((d) => d.id === best.id);
			const rareName = [
				"凡人",
				"炼气",
				"筑基",
				"金丹",
				"元婴",
				"化神",
				"合体",
				"仙人"
			][best.rarity] ?? "";
			if (best.isNew && best.rarity >= 3) get().pushToast(`${def?.name ?? best.id} · ${rareName}拜山`);
			else if (count === 1) get().pushToast(best.isNew ? `${def?.name ?? ""} · 投缘得人` : `${def?.name ?? ""} · 缘印加深`);
			else {
				const news = reveal.filter((r) => r.isNew).length;
				get().pushToast(news > 0 ? `十缘 · 新人 ${news} · 最上${rareName}` : `十缘 · 缘印加深 · 最上${rareName}`);
			}
		}
	},
	clearGacha() {
		set({ gachaReveal: null });
	},
	startExplore(slot, realmId) {
		const s = get();
		const realm = REALMS.find((r) => r.id === realmId);
		if (!realm || s.layer < realm.unlockLayer) return;
		if (s.explores[slot]) return;
		const next = s.explores.slice();
		next[slot] = {
			realmId,
			endAt: Date.now() + realm.duration * 1e3
		};
		set({ explores: next });
	},
	claimExplore(slot) {
		const s = get();
		const job = s.explores[slot];
		if (!job || Date.now() < job.endAt) return;
		const loot = exploreLoot(s, job.realmId);
		s.qi += loot.qi;
		s.jade += loot.jade;
		s.herbs += loot.herbs;
		s.ore += loot.ore;
		s.exploresDone += 1;
		s.explores[slot] = null;
		bumpMissions(s, "explore", 1);
		flush(set, s);
		get().pushToast(`云游归来  香火+${Math.floor(loot.qi)}  缘玉+${loot.jade}`);
		juice.emit({ t: "upgrade" });
	},
	buySkill(id) {
		const s = get();
		const def = SKILLS.find((x) => x.id === id);
		if (!def) return;
		const lv = s.skills[id] ?? 0;
		if (lv >= def.max) return;
		const cost = skillCost(id, lv);
		if (s.daoFruit < cost) return;
		s.daoFruit -= cost;
		s.skills[id] = lv + 1;
		flush(set, s);
		juice.emit({ t: "upgrade" });
		if (s.sfxOn) sfx.upgrade();
	},
	craft(id) {
		const s = get();
		const def = CRAFTS.find((x) => x.id === id);
		if (!def) return;
		if ((s.buildings.alchemy ?? 0) < 1) return;
		const lv = s.crafts[id] ?? 0;
		if (lv >= def.max) return;
		if (s.herbs < def.herbs || s.ore < def.ore || s.jade < def.jade) return;
		s.herbs -= def.herbs;
		s.ore -= def.ore;
		s.jade -= def.jade;
		s.crafts[id] = lv + 1;
		flush(set, s);
		juice.emit({ t: "upgrade" });
		if (s.sfxOn) sfx.upgrade();
	},
	claimMission(id) {
		const s = get();
		const m = s.missions.find((x) => x.id === id);
		if (!m || m.progress < m.target) return;
		s.jade += m.rewardJade;
		s.missions = s.missions.filter((x) => x.id !== id);
		s.missions.push(rollMission(s));
		flush(set, s);
		persistNow(s);
		juice.emit({ t: "mission" });
		juice.emit({
			t: "upgrade",
			name: m.title
		});
		if (s.sfxOn) sfx.orb();
		get().pushToast(`事成 · 缘玉 +${m.rewardJade}`);
	},
	unlockLab() {
		const s = get();
		if (s.labUnlocked || s.jade < 20) return;
		s.jade -= 20;
		s.labUnlocked = true;
		bumpMissions(s, "lab", 1);
		flush(set, s);
		set({ tab: "dao" });
		get().pushToast("藏经阁已开  可修道藏");
		juice.emit({ t: "upgrade" });
	},
	prestige() {
		const s = get();
		const fruit = prestigeFruit(s.totalDamage, multipliers(s).dao);
		if (fruit < 1) return;
		const keepJade = Math.floor(s.jade * .35);
		const next = defaultState();
		next.started = true;
		next.seenTitle = true;
		next.daoFruit = s.daoFruit + fruit;
		next.prestigeCount = s.prestigeCount + 1;
		next.disciples = s.disciples;
		next.skills = s.skills;
		next.crafts = s.crafts;
		next.labUnlocked = s.labUnlocked;
		next.pity = s.pity;
		next.jade = Math.max(keepJade, 10);
		next.qi = startQiAfterPrestige(s);
		next.buildings.hall = 1;
		next.lifetimeDamage = s.lifetimeDamage;
		next.pulls = s.pulls;
		next.sfxOn = s.sfxOn;
		next.shakeOn = s.shakeOn;
		next.tutorialStep = 5;
		next.missions = starterMissions();
		set({
			...get(),
			...next,
			tab: null,
			gachaReveal: null
		});
		juice.emit({ t: "prestige" });
		if (s.sfxOn) sfx.prestige();
		get().pushToast(`劫后归山  道果 +${fruit}`);
		persistNow(get());
	},
	resetSave() {
		const fresh = defaultState();
		set({
			...get(),
			...fresh,
			tab: null,
			gachaReveal: null,
			offlineGift: null,
			toasts: []
		});
		persistNow(get());
	},
	setDebug(v) {
		try {
			localStorage.setItem("xian-debug", v ? "1" : "0");
		} catch {}
		set({ debug: v });
	},
	cheat(kind) {
		const s = get();
		const label = {
			qi: "香火 +100亿",
			jade: "仙玉 +2000",
			fruit: "道果 +100",
			buildings: "全建筑 40 级",
			layer10: "劫层 +10",
			layer50: "直达 50 层",
			unlock: "系统全开",
			prestige: "强行渡劫"
		};
		if (kind === "qi") s.qi += 1e10;
		else if (kind === "jade") s.jade += 2e3;
		else if (kind === "fruit") s.daoFruit += 100;
		else if (kind === "buildings") {
			const next = { ...s.buildings };
			for (const b of BUILDINGS) next[b.id] = Math.max(next[b.id] ?? 0, 40);
			s.buildings = next;
		} else if (kind === "layer10" || kind === "layer50") {
			s.layer = kind === "layer50" ? 50 : s.layer + 10;
			s.layerHp = layerHp(s.layer);
		} else if (kind === "unlock") {
			s.labUnlocked = true;
			s.jade += 2e3;
			if (s.tutorialStep < 5) s.tutorialStep = 5;
		} else if (kind === "prestige") {
			s.daoFruit += 50;
			s.totalDamage = Math.max(s.totalDamage, 2e5);
			s.lifetimeDamage = Math.max(s.lifetimeDamage, 2e5);
			flush(set, s);
			get().prestige();
			return;
		}
		flush(set, s);
		get().pushToast(`测试 · ${label[kind] ?? kind}`);
	},
	setSfx(v) {
		setSfxEnabled(v);
		set({ sfxOn: v });
	},
	setShake(v) {
		set({ shakeOn: v });
	},
	pushToast(text) {
		const id = toastSeq++;
		set({ toasts: [...get().toasts.slice(-3), {
			id,
			text
		}] });
		window.setTimeout(() => {
			set({ toasts: get().toasts.filter((t) => t.id !== id) });
		}, 2600);
	}
}));
if (typeof window !== "undefined") {
	window.__xian = useGame;
	window.addEventListener("visibilitychange", () => {
		if (document.visibilityState === "hidden") persistNow(useGame.getState());
	});
	window.addEventListener("pagehide", () => persistNow(useGame.getState()));
}
//#endregion
//#region src/game/WorldCanvas.tsx
var ASSET_BASE$1 = "/";
var assetUrl$1 = (p) => `${ASSET_BASE$1}${p.replace(/^\//, "")}`;
setInkAssetBase(assetUrl$1("sprites"));
setFxAssetBase(assetUrl$1("fx"));
var BAND_CACHE = [
	buildingsInBand(0),
	buildingsInBand(1),
	buildingsInBand(2)
];
var BUILDING_INDEX = new Map(BUILDINGS.map((b, i) => [b.id, i]));
var SEAL_CHARS = {
	hall: "香",
	house: "舍",
	sword: "剑",
	array: "纹",
	mine: "脉",
	tower: "塔",
	mirror: "镜",
	alchemy: "丹"
};
function drawBuildingLabel(ctx, id, name, x, y, time) {
	const seal = SEAL_CHARS[id];
	if (!seal) return;
	const bob = Math.sin(time * 1.3 + x * .04) * 1.5;
	const cx = x;
	const sealY = y - 6 + bob;
	const nameY = y + 21 + bob;
	ctx.save();
	ctx.textAlign = "center";
	ctx.textBaseline = "middle";
	ctx.beginPath();
	ctx.arc(cx, sealY, 11, 0, Math.PI * 2);
	ctx.fillStyle = "#2e2a26";
	ctx.fill();
	ctx.lineWidth = 1.2;
	ctx.strokeStyle = "rgba(216,198,144,0.55)";
	ctx.stroke();
	ctx.fillStyle = "#e8d9a8";
	ctx.font = "600 13px \"Songti SC\",\"STSong\",serif";
	ctx.fillText(seal, cx, sealY + .5);
	ctx.font = "600 14px \"Songti SC\",\"STSong\",serif";
	ctx.lineWidth = 3;
	ctx.strokeStyle = "rgba(35,32,28,0.85)";
	ctx.strokeText(name, cx, nameY);
	ctx.fillStyle = "#f0e6c8";
	ctx.fillText(name, cx, nameY);
	ctx.restore();
}
function WorldCanvas() {
	const canvasRef = useRef(null);
	useEffect(() => {
		const canvas = canvasRef.current;
		if (!canvas) return;
		const ctx = canvas.getContext("2d");
		if (!ctx) return;
		const particles = [];
		const floaters = [];
		const guests = [];
		let orb = null;
		let trauma = 0;
		let hitStop = 0;
		let frameNo = 0;
		let time = 0;
		let last = performance.now();
		let acc = 0;
		let cachedDps = 0;
		let lastDpsAt = 0;
		let lastStampKey = "";
		let lastStamps = [];
		const getStamps = (st) => {
			const key = st.layer + ":" + JSON.stringify(st.buildings);
			if (key !== lastStampKey) {
				lastStampKey = key;
				lastStamps = stampStates(st.buildings, st.layer);
			}
			return lastStamps;
		};
		let flash = 0;
		let boltFlash = 0;
		let pendingBolt = false;
		let vortexPulse = 0;
		let running = true;
		let orbTimer = 18 + Math.random() * 12;
		let lightningT = 3 + Math.random() * 4;
		let bolt = null;
		const buildingFlash = {};
		const buildingTierUp = {};
		let cssW = 390;
		let cssH = 700;
		const cranes = Array.from({ length: 3 }, (_, i) => ({
			x: -.2 + Math.random() * 1.4,
			y: .03 + Math.random() * .12,
			sp: (.02 + Math.random() * .022) * (i % 2 === 0 ? 1 : -1),
			ph: i * 2.1 + Math.random(),
			sc: .95 + i % 3 * .18
		}));
		const motes = Array.from({ length: 18 }, () => ({
			x: Math.random(),
			y: .2 + Math.random() * .55,
			sp: .008 + Math.random() * .012,
			ph: Math.random() * 10
		}));
		const spawnParticle = (p) => {
			if (particles.length > 200) particles.shift();
			particles.push(p);
		};
		/** 可选触觉：与 shakeOn 同开同关 */
		const pulseVibrate = (ms) => {
			if (!useGame.getState().shakeOn) return;
			try {
				navigator.vibrate?.(ms);
			} catch {}
		};
		const mountain = new Image();
		mountain.crossOrigin = "anonymous";
		mountain.src = assetUrl$1("bg/bg_panorama.jpg?v=8");
		let mountainOk = false;
		const FUSED_BLOCKS = [];
		new Set(FUSED_BLOCKS.map((b) => b.id));
		const fusedImgs = /* @__PURE__ */ new Map();
		for (const b of FUSED_BLOCKS) for (let t = 0; t <= b.tiers; t++) {
			const key = `${b.id}_t${t}`;
			const img = new Image();
			img.crossOrigin = "anonymous";
			img.src = assetUrl$1(`bg/patches/${key}.webp?v=1`);
			fusedImgs.set(key, img);
		}
		let blit = null;
		let mountainCache = null;
		let mountainCacheKey = "";
		mountain.onload = () => {
			mountainOk = true;
			mountainCache = null;
			mountainCacheKey = "";
		};
		const cam = { pan: -1 };
		const clampPan = (w) => {
			const worldW = w * WORLD_K;
			if (cam.pan < 0) cam.pan = worldW * TRIB.nx - w / 2;
			cam.pan = Math.max(0, Math.min(worldW - w, cam.pan));
		};
		const drag = {
			on: false,
			moved: false,
			x: 0,
			y: 0,
			lx: 0
		};
		const LANDINGS = [
			{
				nx: .15,
				ny: .6
			},
			{
				nx: .35,
				ny: .42
			},
			{
				nx: .55,
				ny: .62
			},
			{
				nx: .72,
				ny: .62
			},
			{
				nx: .8,
				ny: .5
			},
			{
				nx: .88,
				ny: .64
			},
			{
				nx: .75,
				ny: .88
			},
			{
				nx: .95,
				ny: .9
			}
		];
		/** 单张空山底图:尺寸变才重建离屏 */
		const ensureMountainCache = (dw, dh, dpr) => {
			const bw = Math.max(1, Math.round(dw * dpr));
			const bh = Math.max(1, Math.round(dh * dpr));
			const key = `qg1|${bw}x${bh}|m:${mountainOk ? 1 : 0}`;
			if (mountainCache && mountainCacheKey === key) return mountainCache;
			if (!(mountainOk && mountain.naturalWidth > 0)) return null;
			const oc = document.createElement("canvas");
			oc.width = bw;
			oc.height = bh;
			const octx = oc.getContext("2d");
			if (!octx) return null;
			octx.imageSmoothingEnabled = true;
			octx.imageSmoothingQuality = "high";
			octx.drawImage(mountain, 0, 0, bw, bh);
			mountainCache = oc;
			mountainCacheKey = key;
			return oc;
		};
		/** 世界比屏宽(WORLD_K 倍):底图 cover 世界矩形,镜头随 pan 平移 */
		const syncBlit = (w, h) => {
			const worldW = w * WORLD_K;
			clampPan(w);
			blit = {
				dx: -cam.pan,
				dy: 0,
				dw: worldW,
				dh: h
			};
			const base = mountainOk && mountain.naturalWidth ? mountain : null;
			if (!base || !base.naturalWidth) return false;
			const ir = base.naturalWidth / base.naturalHeight;
			const wr = worldW / h;
			let dw, dh, dx, dy;
			if (ir > wr) {
				dh = h;
				dw = dh * ir;
				dx = -cam.pan - (dw - worldW) / 2;
				dy = 0;
			} else {
				dw = worldW;
				dh = dw / ir;
				dx = -cam.pan;
				dy = (h - dh) / 2;
			}
			const dpr = canvas.width / Math.max(1, w);
			const cache = ensureMountainCache(dw, dh, dpr);
			if (cache) ctx.drawImage(cache, dx, dy, dw, dh);
			else ctx.drawImage(base, dx, dy, dw, dh);
			const bw = base.naturalWidth;
			const bh = base.naturalHeight;
			const bd = useGame.getState().buildings;
			for (const b of FUSED_BLOCKS) {
				const lv = bd[b.id] ?? 0;
				if (lv <= 0) continue;
				const tier = Math.min(buildingTier(lv), b.tiers);
				const img = fusedImgs.get(`${b.id}_t${tier}`);
				if (!img || img.naturalWidth === 0) continue;
				ctx.drawImage(img, dx + b.sx * dw / bw, dy + b.sy * dh / bh, b.sw * dw / bw, b.sh * dh / bh);
			}
			return true;
		};
		const unsub = juice.on((e) => {
			const w = cssW;
			const h = cssH;
			if (e.t === "click") {
				const x = e.x * w;
				const y = e.y * h;
				trauma = Math.min(1, trauma + (e.crit ? .2 : .1));
				if (e.crit) hitStop = Math.max(hitStop, .06);
				if (e.crit) pulseVibrate(12);
				vortexPulse = 1;
				const n = e.crit ? 9 : 6;
				for (let i = 0; i < n; i++) {
					const a = Math.random() * Math.PI * 2;
					const sp = 16 + Math.random() * 28;
					spawnParticle({
						x,
						y,
						vx: Math.cos(a) * sp,
						vy: Math.sin(a) * sp - 8,
						life: .32 + Math.random() * .22,
						max: .55,
						color: e.crit ? "#7a3a2c" : "#4a443c",
						kind: "mote",
						size: e.crit ? 2.2 : 1.55
					});
				}
				spawnParticle({
					x,
					y,
					vx: 0,
					vy: 0,
					life: .26,
					max: .26,
					color: e.crit ? "#7a3a2c" : "#232429",
					kind: "ring",
					size: e.crit ? 8 : 6
				});
				floaters.push({
					x,
					y: y - 24,
					n: (e.crit ? "暴 " : "+") + formatNum(e.n),
					life: .9,
					crit: e.crit
				});
				if (floaters.length > 18) floaters.shift();
			} else if (e.t === "layer") {
				trauma = Math.min(1, trauma + .65);
				hitStop = Math.max(hitStop, .12);
				flash = .4;
				pendingBolt = true;
				pulseVibrate(18);
				vortexPulse = 1;
				const { cx, cy } = tribCenter(cssW, cssH, blit);
				for (let i = 0; i < 20; i++) {
					const a = i / 20 * Math.PI * 2;
					spawnParticle({
						x: cx,
						y: cy,
						vx: Math.cos(a) * 42,
						vy: Math.sin(a) * 28,
						life: .7,
						max: .7,
						color: i % 2 ? "#7a3a2c" : "#232429",
						kind: "mote",
						size: 2
					});
				}
				floaters.push({
					x: cx,
					y: cy - 22,
					n: `破境 · 第 ${e.layer} 层`,
					life: 1.4,
					crit: true
				});
			} else if (e.t === "orb") {
				orb = null;
				trauma = Math.min(1, trauma + .35);
			} else if (e.t === "upgrade") {
				if (e.id) {
					buildingFlash[e.id] = e.tierUp ? 1.6 : 1.05;
					buildingTierUp[e.id] = !!e.tierUp;
					const slot = BUILDING_SLOTS[e.id];
					if (slot) {
						const p = slotXY(slot.nx, slot.ny, w, h, blit);
						const n = e.tierUp ? 36 : 22;
						for (let i = 0; i < n; i++) {
							const a = i / n * Math.PI * 2;
							const sp = e.tierUp ? 48 : 28;
							spawnParticle({
								x: p.x,
								y: p.y - 10,
								vx: Math.cos(a) * sp,
								vy: Math.sin(a) * sp * .7 - 18,
								life: e.tierUp ? 1.1 : .75,
								max: e.tierUp ? 1.1 : .75,
								color: i % 2 ? "#7a3a2c" : "#ece5d2",
								kind: i % 4 === 0 ? "ring" : "mote",
								size: e.tierUp ? 3.2 : 2.2
							});
						}
						floaters.push({
							x: p.x,
							y: p.y - 28,
							n: e.tierUp ? `进阶 · ${e.name ?? ""}` : `${e.name ?? "建筑"} 升级`,
							life: e.tierUp ? 1.6 : 1.1,
							crit: !!e.tierUp
						});
					}
				}
				trauma = Math.min(1, trauma + (e.tierUp ? .55 : .32));
				flash = Math.max(flash, e.tierUp ? .35 : .16);
			} else if (e.t === "mission") {
				flash = Math.max(flash, .28);
				trauma = Math.min(1, trauma + .2);
				pulseVibrate(10);
				const mx = w * .5;
				const my = h * .42;
				for (let i = 0; i < 14; i++) {
					const a = Math.random() * Math.PI * 2;
					const sp = 8 + Math.random() * 18;
					spawnParticle({
						x: mx + (Math.random() - .5) * 40,
						y: my + (Math.random() - .5) * 24,
						vx: Math.cos(a) * sp,
						vy: Math.sin(a) * sp - 10,
						life: .85 + Math.random() * .45,
						max: 1.3,
						color: i % 3 === 0 ? "#7a3a2c" : i % 2 ? "#4a443c" : "#6b5f4e",
						kind: "mote",
						size: 1.6 + Math.random() * 1.2
					});
				}
				spawnParticle({
					x: mx,
					y: my,
					vx: 0,
					vy: 0,
					life: .5,
					max: .5,
					color: "#7a3a2c",
					kind: "ring",
					size: 6
				});
				floaters.push({
					x: mx,
					y: my - 18,
					n: "事毕",
					life: 1.35,
					crit: true
				});
				if (floaters.length > 18) floaters.shift();
			} else if (e.t === "prestige") {
				flash = .7;
				hitStop = Math.max(hitStop, .15);
				trauma = 1;
				pulseVibrate(22);
			} else if (e.t === "gacha") {
				const def = DISCIPLES.find((d) => d.id === e.id);
				const name = def?.name ?? e.name;
				const aura = def?.aura;
				let guest = guests.find((g) => g.id === e.id);
				const slot = LANDINGS[guests.length % LANDINGS.length];
				if (!guest) {
					guest = {
						id: e.id,
						rarity: e.rarity,
						nx: slot.nx + (Math.random() - .5) * .04,
						ny: slot.ny + (Math.random() - .5) * .02,
						enter: 0,
						enterMax: 1.7,
						fromX: cssW * .78,
						fromY: cssH * .12,
						name,
						v: guests.length % 6,
						bob: Math.random() * 8,
						ang: Math.random() * Math.PI * 2,
						slash: 1.2 + Math.random() * 2
					};
					guests.push(guest);
				}
				if (e.isNew) {
					const enterMax = e.rarity >= 5 ? 2.65 : e.rarity >= 3 ? 2.05 : 1.45;
					guest.enterMax = enterMax;
					guest.enter = enterMax;
					guest.fromX = cssW * (.62 + Math.random() * .28);
					guest.fromY = cssH * (.06 + Math.random() * .08);
					guest.rarity = e.rarity;
					guest.name = name;
					const land = slotXY(guest.nx, guest.ny, cssW, cssH, blit);
					const titleBit = def?.title ? ` · ${def.title}` : "";
					floaters.push({
						x: land.x,
						y: land.y - 26,
						n: e.rarity >= 3 ? `${name}${titleBit}` : `${name} · 拜山`,
						life: e.rarity >= 5 ? 2.35 : e.rarity >= 3 ? 2 : 1.55,
						crit: e.rarity >= 3
					});
					const fxKind = aura ?? (e.rarity >= 6 ? "void" : e.rarity >= 5 ? "sword" : e.rarity >= 3 ? "seal" : "cloud");
					if (e.rarity <= 2) {
						trauma = Math.min(1, trauma + .08);
						for (let i = 0; i < 5; i++) {
							const a = Math.random() * Math.PI * 2;
							const sp = 6 + Math.random() * 10;
							spawnParticle({
								x: land.x,
								y: land.y - 6,
								vx: Math.cos(a) * sp,
								vy: Math.sin(a) * sp * .55 - 4,
								life: .5 + Math.random() * .3,
								max: .85,
								color: i % 2 ? "#6b5f4e" : "#4a443c",
								kind: "mote",
								size: 1.3 + Math.random() * .6
							});
						}
						spawnParticle({
							x: land.x,
							y: land.y - 4,
							vx: 0,
							vy: 0,
							life: .34,
							max: .34,
							color: "#4a443c",
							kind: "ring",
							size: 4
						});
					} else if (e.rarity <= 4) {
						trauma = Math.min(1, trauma + .2);
						flash = Math.max(flash, .14);
						const nBurst = 10 + e.rarity * 2;
						for (let i = 0; i < nBurst; i++) {
							const a = Math.PI * 2 * i / nBurst + Math.random() * .15;
							const sp = fxKind === "sword" ? 26 + Math.random() * 16 : 14 + Math.random() * 14;
							spawnParticle({
								x: land.x,
								y: land.y - 8,
								vx: Math.cos(a) * sp,
								vy: Math.sin(a) * sp * (fxKind === "cloud" ? .4 : .8) - (fxKind === "dawn" ? 12 : 6),
								life: .65 + Math.random() * .35,
								max: 1.05,
								color: fxKind === "seal" || fxKind === "dawn" ? "#7a3a2c" : fxKind === "void" ? "#232429" : "#4a443c",
								kind: fxKind === "sword" ? "sword" : i % 5 === 0 ? "ring" : "mote",
								size: fxKind === "sword" ? 2.5 : fxKind === "seal" ? 4.5 : 2.1
							});
						}
						spawnParticle({
							x: land.x,
							y: land.y - 6,
							vx: 0,
							vy: 0,
							life: .62,
							max: .62,
							color: fxKind === "void" ? "#232429" : "#7a3a2c",
							kind: "ring",
							size: 10
						});
						if (fxKind === "seal") spawnParticle({
							x: land.x,
							y: land.y - 6,
							vx: 0,
							vy: 0,
							life: .42,
							max: .42,
							color: "#7a3a2c",
							kind: "ring",
							size: 6
						});
					} else {
						trauma = Math.min(1, trauma + (e.rarity >= 7 ? .55 : .42));
						flash = Math.max(flash, e.rarity >= 7 ? .55 : .42);
						pulseVibrate(e.rarity >= 7 ? 16 : 12);
						const nBurst = 22 + e.rarity * 4;
						for (let i = 0; i < nBurst; i++) {
							const a = Math.PI * 2 * i / nBurst + Math.random() * .25;
							const sp = fxKind === "sword" ? 36 + Math.random() * 28 : fxKind === "void" ? 22 + Math.random() * 30 : 18 + Math.random() * 22;
							const kind = fxKind === "sword" && i % 3 === 0 ? "sword" : fxKind === "seal" && i % 4 === 0 ? "ring" : "mote";
							spawnParticle({
								x: land.x + (Math.random() - .5) * 18,
								y: land.y - 10 + (Math.random() - .5) * 10,
								vx: Math.cos(a) * sp,
								vy: Math.sin(a) * sp * (fxKind === "cloud" ? .35 : .9) - (fxKind === "dawn" ? 18 : 8),
								life: .85 + Math.random() * .55,
								max: 1.45,
								color: fxKind === "dawn" ? i % 2 ? "#7a3a2c" : "#6b5f4e" : fxKind === "seal" ? i % 2 ? "#7a3a2c" : "#232429" : fxKind === "void" ? i % 3 === 0 ? "#ece5d2" : "#232429" : fxKind === "sword" ? i % 2 ? "#4a443c" : "#232429" : "#4a443c",
								kind,
								size: kind === "sword" ? 3.1 : kind === "ring" ? 6 : 2.4 + Math.random() * 1.4
							});
						}
						spawnParticle({
							x: land.x,
							y: land.y - 8,
							vx: 0,
							vy: 0,
							life: .85,
							max: .85,
							color: fxKind === "dawn" || fxKind === "seal" ? "#7a3a2c" : "#232429",
							kind: "ring",
							size: 14
						});
						spawnParticle({
							x: land.x,
							y: land.y - 8,
							vx: 0,
							vy: 0,
							life: .55,
							max: .55,
							color: "#7a3a2c",
							kind: "ring",
							size: 7
						});
						for (let i = 0; i < 12; i++) spawnParticle({
							x: land.x + (Math.random() - .5) * 50,
							y: land.y - 40 - Math.random() * 30,
							vx: (Math.random() - .5) * 8,
							vy: 18 + Math.random() * 28,
							life: .7 + Math.random() * .4,
							max: 1.2,
							color: i % 4 === 0 ? "#7a3a2c" : "#232429",
							kind: "mote",
							size: 1.2 + Math.random() * 1.8
						});
					}
				}
			}
		});
		const handleTap = (clientX, clientY) => {
			const rect = canvas.getBoundingClientRect();
			const nx = (clientX - rect.left) / rect.width;
			const ny = (clientY - rect.top) / rect.height;
			const st = useGame.getState();
			if (!st.started) return;
			const px = nx * cssW;
			const py = ny * cssH;
			const { cx, cy, R } = tribCenter(cssW, cssH, blit);
			if (orb) {
				const ox = cx + Math.sin(orb.a) * R * .7;
				const oy = cy + Math.cos(orb.a * .65) * R * .32;
				if (Math.hypot(px - ox, py - oy) < 22) {
					st.collectOrb(nx, ny);
					return;
				}
			}
			for (const mark of getStamps(st)) {
				const slot = BUILDING_SLOTS[mark.id];
				if (!slot) continue;
				const p = slotXY(slot.nx, slot.ny, cssW, cssH, blit);
				const box = blit ? blit.dw * Math.max(slot.hw, slot.hh) : 48;
				const hit = Math.max(32, box * .48);
				if (Math.hypot(px - p.x, py - p.y) < hit) {
					if (mark.locked) useGame.getState().pushToast("机缘未至");
					else useGame.getState().openSect(mark.id);
					return;
				}
			}
			st.click(nx, ny);
		};
		const onPointerDown = (ev) => {
			if (!useGame.getState().started) return;
			if (ev.target !== canvas) return;
			drag.on = true;
			drag.moved = false;
			drag.x = ev.clientX;
			drag.y = ev.clientY;
			drag.lx = ev.clientX;
			const rect = canvas.getBoundingClientRect();
			spawnParticle({
				x: (ev.clientX - rect.left) / rect.width * cssW,
				y: (ev.clientY - rect.top) / rect.height * cssH,
				vx: 0,
				vy: 0,
				life: .22,
				max: .22,
				color: "#232429",
				kind: "ring",
				size: 5
			});
		};
		const onPointerMove = (ev) => {
			if (!drag.on) return;
			const dx = ev.clientX - drag.x;
			const dy = ev.clientY - drag.y;
			if (!drag.moved && (Math.abs(dx) > 12 || Math.abs(dy) > 12)) {
				drag.moved = true;
				drag.lx = ev.clientX;
			}
			if (drag.moved) {
				const worldW = cssW * WORLD_K;
				cam.pan = Math.max(0, Math.min(worldW - cssW, cam.pan - (ev.clientX - drag.lx)));
				drag.lx = ev.clientX;
			}
		};
		const onPointerUp = (ev) => {
			if (!drag.on) return;
			const moved = drag.moved;
			drag.on = false;
			if (!moved) handleTap(ev.clientX, ev.clientY);
		};
		const onGlobalDown = (ev) => {
			if (ev.target !== canvas) drag.on = false;
		};
		canvas.addEventListener("pointerdown", onPointerDown);
		window.addEventListener("pointermove", onPointerMove);
		window.addEventListener("pointerup", onPointerUp);
		window.addEventListener("pointercancel", onPointerUp);
		window.addEventListener("pointerdown", onGlobalDown, true);
		const resize = () => {
			const parent = canvas.parentElement;
			if (!parent) return;
			cssW = parent.clientWidth;
			cssH = parent.clientHeight;
			const dpr = Math.min(2, window.devicePixelRatio || 1);
			canvas.width = Math.max(120, Math.floor(cssW * dpr));
			canvas.height = Math.max(180, Math.floor(cssH * dpr));
			canvas.style.width = cssW + "px";
			canvas.style.height = cssH + "px";
			mountainCache = null;
			mountainCacheKey = "";
		};
		resize();
		const ro = new ResizeObserver(resize);
		if (canvas.parentElement) ro.observe(canvas.parentElement);
		const paintBuildings = (band, st, _painted) => {
			const w = cssW;
			const h = cssH;
			for (const def of BAND_CACHE[band]) {
				const slot = BUILDING_SLOTS[def.id];
				if (!slot) continue;
				const lv = st.buildings[def.id] ?? 0;
				const idx = BUILDING_INDEX.get(def.id) ?? 0;
				if (!(idx <= 0 || (st.buildings[BUILDINGS[idx - 1].id] ?? 0) > 0 || def.id === "alchemy" && ((st.buildings.mine ?? 0) > 0 || st.layer >= 8)) && lv <= 0) continue;
				const p = slotXY(slot.nx, slot.ny, w, h, blit);
				const s = blit ? blit.dw * slot.hw : 56;
				if (lv <= 0) drawSiteFx(ctx, p.x, p.y, 0, time, def.id, s, false, band);
				else {
					const fl = buildingFlash[def.id] ?? 0;
					if (fl > 0) drawUpgradeBurst(ctx, p.x, p.y, Math.min(1, fl), !!buildingTierUp[def.id]);
				}
				drawBuildingLabel(ctx, def.id, def.name, p.x, p.y, time);
			}
		};
		let raf = 0;
		let pageVisible = typeof document !== "undefined" ? !document.hidden : true;
		const tick = (now) => {
			if (!running) return;
			if (!pageVisible) {
				raf = 0;
				return;
			}
			try {
				tickFrame(now);
			} catch (err) {
				console.warn(err);
			}
			raf = requestAnimationFrame(tick);
		};
		const onVisibility = () => {
			pageVisible = !document.hidden;
			if (pageVisible && running && raf === 0) {
				last = performance.now();
				raf = requestAnimationFrame(tick);
			}
		};
		document.addEventListener("visibilitychange", onVisibility);
		const tickFrame = (now) => {
			if (!running) return;
			const rawDt = (now - last) / 1e3;
			last = now;
			if (hitStop > 0) hitStop = Math.max(0, hitStop - rawDt);
			let dt = Math.min(rawDt, .1);
			if (hitStop > 0) dt *= .3;
			acc += dt;
			while (acc >= 1 / 30) {
				useGame.getState().tick(1 / 30);
				acc -= 1 / 30;
			}
			frameNo++;
			const renderThis = !useGame.getState().tab || frameNo % 2 === 1;
			time += dt;
			trauma = Math.max(0, trauma - dt * 1.8);
			flash = Math.max(0, flash - dt * 1.6);
			boltFlash = Math.max(0, boltFlash - dt * 2.4);
			vortexPulse = Math.max(0, vortexPulse - dt * 2.4);
			orbTimer -= dt;
			lightningT -= dt;
			for (const k of Object.keys(buildingFlash)) buildingFlash[k] = Math.max(0, buildingFlash[k] - dt * 1.15);
			const w = cssW;
			const h = cssH;
			const st = useGame.getState();
			if (now - lastDpsAt > 500) {
				cachedDps = totalDps(st);
				lastDpsAt = now;
			}
			const dps = cachedDps;
			const { cx, cy, R } = tribCenter(w, h, blit);
			if (orbTimer < 0 && !orb && st.started) {
				orb = {
					a: Math.random() * Math.PI * 2,
					life: 11
				};
				orbTimer = (22 + Math.random() * 16) / (1 + .04 * (st.buildings.mirror ?? 0));
			}
			if (orb) {
				orb.life -= dt;
				orb.a += dt * .7;
				if (orb.life <= 0) orb = null;
			}
			/** 雷劫：从劫云劈下分叉闪电，落点对准目标；big=破境大雷 */
			const strikeAt = (tx, ty, big) => {
				const pts = [];
				const branches = [];
				let x = cx + (Math.random() - .5) * R * .9;
				let y = cy + R * .22;
				pts.push({
					x,
					y
				});
				const segs = big ? 9 : 7;
				for (let i = 0; i < segs; i++) {
					x += (tx - x) * .24 + (Math.random() - .5) * 26;
					y += (ty - y) * .24 + (Math.random() - .35) * 10;
					pts.push({
						x,
						y
					});
					if (Math.random() < (big ? .6 : .45)) {
						const bx = x + (Math.random() - .5) * 44;
						const by = y + 14 + Math.random() * 26;
						branches.push([
							{
								x,
								y
							},
							{
								x: (x + bx) / 2 + (Math.random() - .5) * 10,
								y: (y + by) / 2
							},
							{
								x: bx,
								y: by
							}
						]);
					}
				}
				pts.push({
					x: tx,
					y: ty
				});
				bolt = {
					pts,
					branches,
					life: big ? .3 : .22
				};
				boltFlash = Math.max(boltFlash, big ? .5 : .26);
				trauma = Math.min(1, trauma + (big ? .3 : .15));
			};
			/** 落点：优先劈向已解锁建筑，否则劈向劫云正下方地面 */
			const strikeRandomTarget = (big) => {
				const st2 = useGame.getState();
				const ownedIds = BUILDINGS.map((b) => b.id).filter((id) => (st2.buildings[id] ?? 0) > 0);
				let tx;
				let ty;
				if (ownedIds.length > 0 && Math.random() < .7) {
					const slot = BUILDING_SLOTS[ownedIds[Math.floor(Math.random() * ownedIds.length)]];
					const p = slot ? slotXY(slot.nx, slot.ny, w, h, blit) : null;
					tx = (p ? p.x : w * .5) + (Math.random() - .5) * 20;
					ty = (p ? p.y : h * .7) - 12;
				} else {
					tx = cx + (Math.random() - .5) * R * 2;
					ty = cy + R * 2.4 + Math.random() * h * .08;
				}
				strikeAt(tx, ty, big);
			};
			if (pendingBolt) {
				pendingBolt = false;
				strikeRandomTarget(true);
			}
			if (lightningT < 0) {
				lightningT = 5 + Math.random() * 7;
				strikeRandomTarget(false);
			}
			if (bolt) {
				bolt.life -= dt;
				if (bolt.life <= 0) bolt = null;
			}
			const owned = BUILDINGS.filter((b) => (st.buildings[b.id] ?? 0) > 0);
			const fireRate = Math.min(12, 1 + Math.log10(dps + 1) * 2.6);
			if (dps > 0 && owned.length && Math.random() < fireRate * dt) {
				const def = owned[Math.floor(Math.random() * owned.length)] ?? BUILDINGS[0];
				const slot = BUILDING_SLOTS[def.id];
				const origin = slot ? slotXY(slot.nx, slot.ny, w, h, blit) : {
					x: w * .5,
					y: h * .75
				};
				const sx = origin.x;
				const sy = origin.y;
				const dx = cx - sx;
				const dy = cy - sy;
				const len = Math.hypot(dx, dy) || 1;
				const inkish = def.color === "#a63d32" || def.color === "#6e2a24" ? def.color : def.projectile === "sword" ? "#4a443c" : "#232429";
				spawnParticle({
					x: sx,
					y: sy,
					vx: dx / len * 48,
					vy: dy / len * 48,
					life: .95,
					max: .95,
					color: inkish,
					kind: def.projectile === "sword" ? "sword" : "mote",
					size: def.projectile === "sword" ? 2.2 : 1.9
				});
			}
			const ownedIds = new Set(st.disciples.map((d) => d.id));
			for (let i = guests.length - 1; i >= 0; i--) if (!ownedIds.has(guests[i].id)) guests.splice(i, 1);
			for (const d of st.disciples) {
				if (guests.some((g) => g.id === d.id)) continue;
				const def = DISCIPLES.find((x) => x.id === d.id);
				const slot = LANDINGS[guests.length % LANDINGS.length];
				guests.push({
					id: d.id,
					rarity: def?.rarity ?? 0,
					nx: slot.nx + (Math.random() - .5) * .04,
					ny: slot.ny + (Math.random() - .5) * .02,
					enter: 0,
					enterMax: 1.7,
					fromX: 0,
					fromY: 0,
					name: def?.name ?? "",
					v: guests.length % 6,
					bob: Math.random() * 8,
					ang: Math.random() * Math.PI * 2,
					slash: 1.2 + Math.random() * 2
				});
			}
			for (const g of guests) {
				if (g.enter > 0) g.enter = Math.max(0, g.enter - dt);
				if (g.rarity >= 3 && g.enter <= 0) {
					g.ang += dt * (.45 + g.rarity * .05);
					g.slash -= dt;
					if (g.slash <= 0) {
						g.slash = 2.4 + Math.random() * 1.4;
						spawnParticle({
							x: cx,
							y: cy,
							vx: Math.cos(g.ang) * 40,
							vy: Math.sin(g.ang) * 20,
							life: .45,
							max: .45,
							color: "#7a3a2c",
							kind: "sword",
							size: 3
						});
						vortexPulse = Math.max(vortexPulse, .5);
					}
				}
			}
			for (const b of cranes) {
				b.x += b.sp * dt;
				b.ph += dt;
				if (b.x > 1.28) {
					b.x = -.24;
					b.y = .02 + Math.random() * .14;
					b.sc = 1.1 + Math.random() * .55;
				} else if (b.x < -.24) {
					b.x = 1.28;
					b.y = .02 + Math.random() * .14;
					b.sc = 1.1 + Math.random() * .55;
				}
			}
			for (const m of motes) {
				m.y -= m.sp * dt * .35;
				m.x += Math.sin(time * .4 + m.ph) * dt * .02;
				if (m.y < .12) {
					m.y = .78;
					m.x = Math.random();
				}
			}
			for (let i = particles.length - 1; i >= 0; i--) {
				const p = particles[i];
				p.life -= dt;
				p.x += p.vx * dt;
				p.y += p.vy * dt;
				p.vx *= .97;
				p.vy *= .97;
				if (p.life <= 0) particles.splice(i, 1);
			}
			for (let i = floaters.length - 1; i >= 0; i--) {
				const f = floaters[i];
				f.life -= dt;
				f.y -= 22 * dt;
				if (f.life <= 0) floaters.splice(i, 1);
			}
			const shake = trauma * trauma;
			if (renderThis) {
				const ox = st.shakeOn ? Math.sin(time * 38) * 5 * shake : 0;
				const oy = st.shakeOn ? Math.cos(time * 31) * 4 * shake : 0;
				const dpr = canvas.width / Math.max(1, w);
				ctx.setTransform(dpr, 0, 0, dpr, ox * dpr, oy * dpr);
				ctx.imageSmoothingEnabled = true;
				ctx.imageSmoothingQuality = "high";
				drawSky(ctx, w, h);
				const painted = syncBlit(w, h);
				for (const crane of cranes) {
					const p = slotXY(crane.x, crane.y, w, h, blit);
					const by = p.y + Math.sin(time * 1.1 + crane.ph) * 7;
					const face = crane.sp >= 0 ? 1 : -1;
					ctx.save();
					for (let k = 1; k <= 3; k++) {
						ctx.globalAlpha = .07 / k;
						ctx.fillStyle = "#232429";
						ctx.beginPath();
						ctx.arc(p.x - face * k * 8 * crane.sc, by + Math.sin(time + crane.ph + k) * 1.5, 1.4 / k, 0, Math.PI * 2);
						ctx.fill();
					}
					ctx.restore();
					drawCraneFly(ctx, p.x, by, time + crane.ph, 46 * crane.sc, face);
				}
				for (const m of motes) {
					ctx.globalAlpha = .14 + .1 * Math.sin(time + m.ph);
					ctx.fillStyle = "#232429";
					ctx.beginPath();
					ctx.arc(m.x * w, m.y * h, 1.1, 0, Math.PI * 2);
					ctx.fill();
					ctx.globalAlpha = 1;
				}
				if (!painted) {
					drawDistantPeaks(ctx, w, h);
					drawPeaks(ctx, w, h, 0);
				}
				drawTribCloud(ctx, cx, cy, R * (1.18 + vortexPulse * .07), vortexPulse, time);
				paintBuildings(0, st, painted);
				if (!painted) {
					drawPeaks(ctx, w, h, 1);
					drawWaterfall(ctx, w, h, time);
				}
				paintBuildings(1, st, painted);
				if (!painted) drawPeaks(ctx, w, h, 2);
				paintBuildings(2, st, painted);
				if (!painted) {
					drawBandMist(ctx, w, h, 2);
					drawForeground(ctx, w, h);
				}
				const focus = st.focusBuilding;
				for (const mark of getStamps(st)) {
					const slot = BUILDING_SLOTS[mark.id];
					if (!slot) continue;
					const p = slotXY(slot.nx, slot.ny, w, h, blit);
					if (focus === mark.id) {
						const pulse = .45 + .35 * Math.sin(time * 4);
						ctx.strokeStyle = `rgba(166,61,50,${pulse})`;
						ctx.lineWidth = 2;
						ctx.beginPath();
						ctx.arc(p.x, p.y, 18 + Math.sin(time * 4) * 2, 0, Math.PI * 2);
						ctx.stroke();
					}
				}
				const shownGuests = guests.slice(0, 8);
				for (const g of shownGuests) {
					const land = slotXY(g.nx, g.ny, w, h, blit);
					if (g.enter > 0) {
						const em = g.enterMax > 0 ? g.enterMax : 1.7;
						const u = 1 - g.enter / em;
						const ease = 1 - (1 - u) * (1 - u) * (1 - u);
						const x = g.fromX + (land.x - g.fromX) * ease;
						const y = g.fromY + (land.y - g.fromY) * ease;
						drawSwordRider(ctx, x, y, time + g.bob, 1.15, land.x >= g.fromX ? 1 : -1, g.rarity);
						ctx.save();
						ctx.font = "700 14px 'Noto Serif SC', serif";
						ctx.textAlign = "center";
						ctx.strokeStyle = "rgba(243,238,228,0.85)";
						ctx.lineWidth = 4;
						ctx.strokeText(g.name, x, y - 18);
						ctx.fillStyle = "#232429";
						ctx.fillText(g.name, x, y - 18);
						ctx.restore();
					} else if (g.rarity >= 3) {
						const rad = R * (2.35 - (g.slash < .28 ? 1 - g.slash / .28 : 0) * .35);
						const x = cx + Math.cos(g.ang) * rad;
						const y = cy + Math.sin(g.ang) * R * .72 + 10;
						drawSwordRider(ctx, x, y, time + g.bob, 1.55, Math.cos(g.ang) > 0 ? 1 : -1, g.rarity);
					} else drawCultivator(ctx, land.x, land.y, g.v, time + g.bob, 1.45, 1);
				}
				if (bolt) {
					const a = Math.min(1, bolt.life * 5);
					ctx.lineJoin = "round";
					ctx.lineCap = "round";
					const passes = [
						[`rgba(140,190,255,${a * .35})`, 7],
						[`rgba(225,240,255,${a * .85})`, 3],
						[`rgba(255,255,255,${a})`, 1.2]
					];
					const traceBolt = () => {
						ctx.beginPath();
						ctx.moveTo(bolt.pts[0].x, bolt.pts[0].y);
						for (const p of bolt.pts) ctx.lineTo(p.x, p.y);
						for (const br of bolt.branches) {
							ctx.moveTo(br[0].x, br[0].y);
							for (let i = 1; i < br.length; i++) ctx.lineTo(br[i].x, br[i].y);
						}
					};
					for (const [style, lw] of passes) {
						ctx.strokeStyle = style;
						ctx.lineWidth = lw;
						traceBolt();
						ctx.stroke();
					}
					const tip = bolt.pts[bolt.pts.length - 1];
					ctx.strokeStyle = `rgba(225,240,255,${a * .8})`;
					ctx.lineWidth = 2;
					ctx.beginPath();
					ctx.arc(tip.x, tip.y, (1 - a) * 30 + 6, 0, Math.PI * 2);
					ctx.stroke();
				}
				if (orb) {
					const oxp = cx + Math.sin(orb.a) * R * .85;
					const oyp = cy + Math.cos(orb.a * .65) * R * .28;
					const pulse = 5 + Math.sin(time * 5) * 1.2;
					ctx.fillStyle = "rgba(166,61,50,0.2)";
					ctx.beginPath();
					ctx.arc(oxp, oyp, pulse + 6, 0, Math.PI * 2);
					ctx.fill();
					ctx.fillStyle = "#7a3a2c";
					ctx.beginPath();
					ctx.arc(oxp, oyp, pulse, 0, Math.PI * 2);
					ctx.fill();
					ctx.fillStyle = "#ece5d2";
					ctx.fillRect(Math.round(oxp) - 1, Math.round(oyp) - 1, 2, 2);
				}
				for (const p of particles) {
					const a = Math.max(0, p.life / p.max);
					if (p.kind === "ring") {
						ctx.strokeStyle = p.color.startsWith("#a6") ? `rgba(166,61,50,${a * .85})` : `rgba(28,25,20,${a * .75})`;
						ctx.lineWidth = 1.25;
						ctx.beginPath();
						ctx.arc(p.x, p.y, (1 - a) * (26 + p.size) + 4, 0, Math.PI * 2);
						ctx.stroke();
					} else if (p.kind === "sword") {
						ctx.save();
						ctx.translate(p.x, p.y);
						ctx.rotate(Math.atan2(p.vy, p.vx));
						ctx.globalAlpha = a;
						ctx.fillStyle = p.color;
						ctx.fillRect(0, -1, 11, 2);
						ctx.fillStyle = "#ece5d2";
						ctx.fillRect(9, -2, 3, 4);
						ctx.restore();
					} else if (p.kind === "mote") {
						ctx.globalAlpha = a;
						ctx.fillStyle = p.color;
						ctx.beginPath();
						ctx.ellipse(p.x, p.y, p.size, p.size * .55, Math.atan2(p.vy, p.vx), 0, Math.PI * 2);
						ctx.fill();
						ctx.globalAlpha = 1;
					} else {
						ctx.globalAlpha = a * .75;
						ctx.fillStyle = p.color;
						ctx.beginPath();
						ctx.ellipse(p.x, p.y, p.size * 1.35, p.size * .7, 0, 0, Math.PI * 2);
						ctx.fill();
						ctx.globalAlpha = a * .35;
						ctx.beginPath();
						ctx.ellipse(p.x, p.y, p.size * 2.2, p.size * 1.2, 0, 0, Math.PI * 2);
						ctx.fill();
						ctx.globalAlpha = 1;
					}
				}
				ctx.font = "700 13px 'Noto Serif SC', serif";
				ctx.textAlign = "center";
				ctx.strokeStyle = "rgba(28,25,20,0.55)";
				ctx.lineWidth = 4;
				ctx.strokeText(formatNum(Math.max(0, st.layerHp)), cx, cy + R * .82);
				ctx.fillStyle = "#ece5d2";
				ctx.fillText(formatNum(Math.max(0, st.layerHp)), cx, cy + R * .82);
				ctx.font = "600 13px 'Noto Sans SC', sans-serif";
				for (const f of floaters) {
					ctx.globalAlpha = Math.max(0, f.life);
					if (f.crit) {
						ctx.lineWidth = 3;
						ctx.strokeStyle = "rgba(236,229,210,0.9)";
						ctx.strokeText(f.n, f.x, f.y);
					}
					ctx.fillStyle = f.crit ? "#7a3a2c" : "#232429";
					ctx.fillText(f.n, f.x, f.y);
					ctx.globalAlpha = 1;
				}
				if (flash > 0) {
					ctx.fillStyle = `rgba(242,250,246,${flash * .32})`;
					ctx.fillRect(-4, -4, w + 8, h + 8);
				}
				if (boltFlash > 0) {
					ctx.fillStyle = `rgba(235,244,252,${boltFlash * .5})`;
					ctx.fillRect(-4, -4, w + 8, h + 8);
				}
			}
		};
		raf = requestAnimationFrame(tick);
		return () => {
			running = false;
			cancelAnimationFrame(raf);
			raf = 0;
			document.removeEventListener("visibilitychange", onVisibility);
			unsub();
			canvas.removeEventListener("pointerdown", onPointerDown);
			window.removeEventListener("pointermove", onPointerMove);
			window.removeEventListener("pointerup", onPointerUp);
			window.removeEventListener("pointercancel", onPointerUp);
			window.removeEventListener("pointerdown", onGlobalDown, true);
			ro.disconnect();
		};
	}, []);
	return /* @__PURE__ */ jsx("canvas", {
		ref: canvasRef,
		className: "absolute inset-0 z-0 h-full w-full touch-none"
	});
}
//#endregion
//#region src/game/ui.tsx
var ASSET_BASE = "/";
var assetUrl = (p) => `${ASSET_BASE}${p.replace(/^\//, "")}`;
/** 壳层稀有度：墨深 + 高阶朱砂，不用彩虹档色 */
var INK_RARITY_COLOR = [
	"#9a9488",
	"#7a7468",
	"#5c564c",
	"#3a3630",
	"#a63d32",
	"#8a3028",
	"#6e2a24",
	"#4a1e1a"
];
function inkRarityColor(r) {
	return INK_RARITY_COLOR[Math.max(0, Math.min(7, r | 0))] ?? INK_RARITY_COLOR[0];
}
var TAB_AXIS = {
	fate: "拜山卷",
	mind: "开光卷",
	sect: "山门对照",
	realm: "云游卷",
	dao: "道藏卷"
};
function buildingBreath(state, b, lv) {
	if (lv <= 0) return b.baseDps;
	const m = multipliers(state);
	const hallMul = 1 + .005 * (state.buildings.hall ?? 0);
	const swordExtra = 1 + .016 * (state.buildings.sword ?? 0);
	const arrayMul = 1 + .012 * (state.buildings.array ?? 0);
	const sword = b.projectile === "sword" ? m.sword * swordExtra : 1;
	const layer = b.id === "array" ? arrayMul : 1;
	return b.baseDps * lv * m.dps * sword * hallMul * layer;
}
function lockHint(bId, unlocked) {
	if (unlocked) return "";
	if (bId === "alchemy") return "机缘未至 · 脉口已开或八层后可显丹灶";
	return "机缘未至";
}
/** HUD 任务进度：开光累计等 damage 浮点不拖长串；大数走 formatNum */
function formatMissionProgress(progress, target) {
	const p = Math.min(Math.max(0, progress), target);
	const t = Math.max(0, target);
	if (t >= 100) return `${formatNum(Math.floor(p))} / ${formatNum(Math.floor(t))}`;
	if (t > 0 && t < 20 && (p % 1 !== 0 || t % 1 !== 0)) return `${Math.min(100, Math.floor(p / t * 100))}%`;
	return `${Math.floor(p)} / ${Math.floor(t)}`;
}
function Hud() {
	const qi = useGame((s) => s.qi);
	const jade = useGame((s) => s.jade);
	const herbs = useGame((s) => s.herbs);
	const ore = useGame((s) => s.ore);
	const combo = useGame((s) => s.combo);
	const missions = useGame((s) => s.missions);
	const lab = useGame((s) => s.labUnlocked);
	const disciples = useGame((s) => s.disciples);
	const dps = useGame((s) => totalDps(s));
	const mission = missions.find((m) => m.progress < m.target) ?? missions[0];
	const ready = missions.find((m) => m.progress >= m.target);
	const portrait = disciples[0] ? portraitSrc(DISCIPLES.find((d) => d.id === disciples[0].id) ?? "outer") : assetUrl("sprites/mascot.png?v=2");
	return /* @__PURE__ */ jsxs("div", {
		className: "pointer-events-none absolute inset-0 z-10",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "pointer-events-auto flex items-stretch gap-2.5 px-3 pt-[max(12px,env(safe-area-inset-top))]",
				children: [
					/* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: () => useGame.getState().setMenu(true),
						className: "hud-stat flex h-14 w-14 shrink-0 items-center justify-center overflow-hidden p-0",
						"aria-label": "宗主",
						children: /* @__PURE__ */ jsx("img", {
							src: portrait,
							alt: "",
							className: "h-full w-full object-cover object-top"
						})
					}),
					ready ? /* @__PURE__ */ jsxs("button", {
						type: "button",
						onClick: () => useGame.getState().claimMission(ready.id),
						className: "hud-stat flex min-h-14 min-w-0 flex-1 items-center justify-between gap-2 px-4 text-left",
						children: [/* @__PURE__ */ jsx("span", {
							className: "truncate text-base font-medium text-ink",
							children: ready.title
						}), /* @__PURE__ */ jsxs("span", {
							className: "shrink-0 text-base font-semibold text-seal",
							children: ["领取 · 玉×", ready.rewardJade]
						})]
					}) : !lab && jade >= 20 ? /* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: () => useGame.getState().unlockLab(),
						className: "hud-stat flex min-h-14 min-w-0 flex-1 items-center px-4 text-left text-base font-medium text-ink",
						children: "解锁藏经阁"
					}) : mission ? /* @__PURE__ */ jsxs("div", {
						className: "hud-stat flex min-h-14 min-w-0 flex-1 items-center justify-between gap-2 px-4",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "min-w-0 flex-1",
							children: [/* @__PURE__ */ jsx("div", {
								className: "truncate text-base text-ink",
								children: mission.title
							}), /* @__PURE__ */ jsxs("div", {
								className: "mt-1 flex items-center gap-2",
								children: [/* @__PURE__ */ jsx("div", {
									className: "meter meter-red min-w-0 flex-1",
									children: /* @__PURE__ */ jsx("i", { style: { width: `${Math.min(100, mission.progress / Math.max(1, mission.target) * 100)}%` } })
								}), /* @__PURE__ */ jsx("div", {
									className: "shrink-0 text-xs tabular-nums text-muted",
									children: formatMissionProgress(mission.progress, mission.target)
								})]
							})]
						}), /* @__PURE__ */ jsxs("span", {
							className: "shrink-0 text-sm font-medium text-seal",
							children: ["玉×", mission.rewardJade]
						})]
					}) : /* @__PURE__ */ jsx("div", { className: "hud-stat min-h-14 flex-1" }),
					/* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: () => useGame.getState().setMenu(true),
						className: "hud-stat flex h-14 w-14 shrink-0 items-center justify-center",
						"aria-label": "菜单",
						children: /* @__PURE__ */ jsx("svg", {
							width: "22",
							height: "22",
							viewBox: "0 0 18 18",
							fill: "none",
							className: "stroke-ink",
							"aria-hidden": true,
							children: /* @__PURE__ */ jsx("path", {
								d: "M3 5h12M3 9h12M3 13h12",
								strokeWidth: "1.6"
							})
						})
					})
				]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "pointer-events-none absolute inset-x-0 top-[4.5rem]",
				children: /* @__PURE__ */ jsxs("div", {
					className: "ink-topbar flex items-center gap-4 overflow-x-auto px-4 py-2",
					children: [
						/* @__PURE__ */ jsxs("span", {
							className: "topbar-pair",
							children: [/* @__PURE__ */ jsx("span", {
								className: "topbar-ico seal",
								children: "香"
							}), /* @__PURE__ */ jsx("span", {
								className: "tp-num",
								children: formatNum(qi)
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: "topbar-pair",
							children: [/* @__PURE__ */ jsx("span", {
								className: "topbar-ico",
								children: "息"
							}), /* @__PURE__ */ jsxs("span", {
								className: "tp-num",
								children: [formatNum(dps), "/秒"]
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: "topbar-pair",
							children: [/* @__PURE__ */ jsx("span", {
								className: "topbar-ico",
								children: "玉"
							}), /* @__PURE__ */ jsx("span", {
								className: "tp-num",
								children: formatNum(jade)
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: "topbar-pair",
							children: [/* @__PURE__ */ jsx("span", {
								className: "topbar-ico",
								children: "草"
							}), /* @__PURE__ */ jsx("span", {
								className: "tp-num",
								children: formatNum(Math.floor(herbs))
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: "topbar-pair",
							children: [/* @__PURE__ */ jsx("span", {
								className: "topbar-ico",
								children: "矿"
							}), /* @__PURE__ */ jsx("span", {
								className: "tp-num",
								children: formatNum(Math.floor(ore))
							})]
						})
					]
				})
			}),
			combo > 2 && /* @__PURE__ */ jsxs("div", {
				className: "pointer-events-none absolute left-4 top-32 font-display text-4xl text-seal",
				children: [combo, " 开光"]
			})
		]
	});
}
var TICKER_LINES = [
	"【世界】剑九：悬剑冢今日第三十九次鸣剑，劫云退了三丈",
	"【世界】岚织：云阶灯已点亮七盏，诚邀同参",
	"【世界】位面通告：合抱之木，生于毫末；修道之行，始于足下",
	"【世界】赤云真人：瀑侧丹灶新开一炉，香火正旺",
	"【世界】拾烬：云游归来，半块残纹换三两香火",
	"【世界】玄天老祖：守峰三千年，今日山门又添一盏灯"
];
function WorldTicker() {
	const [i, setI] = useState(0);
	useEffect(() => {
		const t = window.setInterval(() => setI((v) => (v + 1) % TICKER_LINES.length), 6e3);
		return () => window.clearInterval(t);
	}, []);
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none absolute inset-x-0 bottom-full z-30 px-4 pb-2",
		children: /* @__PURE__ */ jsx("div", {
			className: "world-ticker",
			children: TICKER_LINES[i]
		}, i)
	});
}
function BottomNav() {
	const tab = useGame((s) => s.tab);
	const tutorialStep = useGame((s) => s.tutorialStep);
	const explores = useGame((s) => s.explores);
	const now = Date.now();
	const realmReady = explores.some((slot) => slot && now >= slot.endAt);
	return /* @__PURE__ */ jsxs("nav", {
		className: "pointer-events-auto absolute inset-x-0 bottom-0 z-40 mx-auto max-w-lg",
		children: [/* @__PURE__ */ jsx(WorldTicker, {}), /* @__PURE__ */ jsx("div", {
			className: "seal-nav",
			children: [
				{
					id: "fate",
					label: "拜山",
					mark: "缘"
				},
				{
					id: "mind",
					label: "开光",
					mark: "识"
				},
				{
					id: "sect",
					label: "仙山",
					mark: "山"
				},
				{
					id: "realm",
					label: "云游",
					mark: "游"
				},
				{
					id: "dao",
					label: "道藏",
					mark: "道"
				}
			].map((it) => {
				const on = tab === it.id;
				const dot = realmReady && it.id === "realm";
				const pulse = tutorialStep === 3 && it.id === "fate";
				return /* @__PURE__ */ jsxs("button", {
					type: "button",
					onClick: () => useGame.getState().setTab(it.id),
					className: `seal-chip ${on ? "seal-chip-on" : ""}`,
					"aria-label": it.label,
					children: [/* @__PURE__ */ jsxs("span", {
						className: `seal-disc ${pulse ? "nav-pulse" : ""}`,
						children: [it.mark, dot && /* @__PURE__ */ jsx("span", {
							className: "seal-dot",
							"aria-hidden": true
						})]
					}), /* @__PURE__ */ jsx("span", {
						className: "seal-chip-label",
						children: it.label
					})]
				}, it.id);
			})
		})]
	});
}
function Sheet() {
	const tab = useGame((s) => s.tab);
	if (!tab) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-auto absolute inset-x-0 bottom-28 z-30 mx-auto max-w-lg px-3 pb-1",
		children: /* @__PURE__ */ jsxs("div", {
			className: "sheet-enter hud-box max-h-[62vh] overflow-y-auto bg-paper p-5",
			"data-allow-touch-scroll": true,
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "sheet-axis sticky top-0 z-10 bg-paper/95",
					children: [/* @__PURE__ */ jsx("div", {
						className: "panel-title",
						children: TAB_AXIS[tab] ?? "对照卷"
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						onClick: () => useGame.getState().setTab(tab),
						className: "flex min-h-11 items-center px-3 text-base tracking-widest text-muted",
						children: "收起"
					})]
				}),
				tab === "sect" && /* @__PURE__ */ jsx(SectPanel, {}),
				tab === "mind" && /* @__PURE__ */ jsx(MindPanel, {}),
				tab === "fate" && /* @__PURE__ */ jsx(FatePanel, {}),
				tab === "realm" && /* @__PURE__ */ jsx(RealmPanel, {}),
				tab === "dao" && /* @__PURE__ */ jsx(DaoPanel, {})
			]
		})
	});
}
function BuyToggle() {
	const mode = useGame((s) => s.buyMode);
	return /* @__PURE__ */ jsx("div", {
		className: "flex gap-1.5",
		children: [
			1,
			10,
			100,
			"max"
		].map((o) => /* @__PURE__ */ jsx("button", {
			type: "button",
			onClick: () => useGame.getState().setBuyMode(o),
			className: `min-h-11 min-w-12 rounded-xl px-3 text-sm font-semibold ${mode === o ? "bg-ink text-paper" : "bg-paper-2 text-muted"}`,
			children: o === "max" ? "最大" : `×${o}`
		}, String(o)))
	});
}
function SectPanel() {
	const qi = useGame((s) => s.qi);
	const buildings = useGame((s) => s.buildings);
	const mode = useGame((s) => s.buyMode);
	const layer = useGame((s) => s.layer);
	const focus = useGame((s) => s.focusBuilding);
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "mb-3 flex items-center justify-between",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "panel-title",
				children: "仙山对照"
			}), /* @__PURE__ */ jsx(BuyToggle, {})]
		}),
		/* @__PURE__ */ jsxs("p", {
			className: "mb-3 text-sm text-muted",
			children: [
				"天道第 ",
				layer + 1,
				" 层 · 主购在山体碑亭，此卷快捷升级"
			]
		}),
		[
			{
				band: 2,
				title: "山麓 · 烟火"
			},
			{
				band: 1,
				title: "山腰 · 剑阵"
			},
			{
				band: 0,
				title: "近劫 · 接天"
			}
		].map(({ band, title }) => {
			const list = buildingsInBand(band);
			if (!list.length) return null;
			return /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
				className: "ink-divider",
				children: /* @__PURE__ */ jsx("span", { children: title })
			}), /* @__PURE__ */ jsx("ul", {
				className: "flex flex-col gap-2.5",
				children: list.map((b) => {
					const i = BUILDINGS.findIndex((x) => x.id === b.id);
					const unlocked = buildingUnlocked(useGame.getState(), i);
					const lv = buildings[b.id] ?? 0;
					const n = mode === "max" ? Math.max(1, maxAffordable(b.id, lv, qi)) : mode;
					const cost = buildingCost(b.id, lv, n);
					const can = unlocked && qi >= cost;
					const tier = buildingTier(lv);
					const next = nextTierAt(lv);
					return /* @__PURE__ */ jsxs("li", {
						className: `shop-row flex items-center gap-3 p-3.5 ${unlocked ? "" : "shop-row-locked opacity-70"} ${focus === b.id ? "ring-2 ring-seal" : ""}`,
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "relative h-16 w-16 shrink-0 overflow-hidden rounded-xl border border-ink/10 bg-paper",
								children: [/* @__PURE__ */ jsx("img", {
									src: `/sprites/${b.sprite}_t1.png`,
									alt: "",
									className: "h-full w-full object-cover object-top"
								}), tier > 0 && /* @__PURE__ */ jsx("span", {
									className: "absolute bottom-0 right-0 rounded-tl-lg bg-ink px-1.5 text-xs leading-5 text-paper",
									children: tierName(b.id, tier)
								})]
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "min-w-0 flex-1",
								children: [
									/* @__PURE__ */ jsxs("div", {
										className: "flex items-baseline justify-between gap-2",
										children: [/* @__PURE__ */ jsx("span", {
											className: "shop-row-name text-base font-semibold text-ink",
											children: b.name
										}), /* @__PURE__ */ jsxs("span", {
											className: "text-sm tabular-nums text-muted",
											children: ["Lv.", lv]
										})]
									}),
									/* @__PURE__ */ jsx("div", {
										className: "shop-row-sub line-clamp-1 text-sm text-faint",
										children: unlocked ? b.flavor : lockHint(b.id, unlocked)
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "mt-0.5 text-sm tabular-nums text-seal",
										children: [
											formatNum(buildingBreath(useGame.getState(), b, Math.max(1, lv))),
											" 山息/秒",
											lv > 0 ? ` · ${perkLine(b.id, lv)}` : "",
											next ? ` · ${next - lv}级进阶` : ""
										]
									})
								]
							}),
							/* @__PURE__ */ jsx("button", {
								type: "button",
								disabled: !can,
								onClick: () => useGame.getState().buyBuilding(b.id),
								className: "buy-btn min-h-11 shrink-0 px-4 text-sm leading-tight",
								children: unlocked ? formatNum(cost) : "未开"
							})
						]
					}, b.id);
				})
			})] }, band);
		})
	] });
}
function MindPanel() {
	const qi = useGame((s) => s.qi);
	const clickUp = useGame((s) => s.clickUp);
	const power = useGame((s) => clickPower(s));
	const cc = useGame((s) => critChance(s));
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx("h2", {
			className: "panel-title mb-1",
			children: "开光"
		}),
		/* @__PURE__ */ jsxs("p", {
			className: "mb-4 text-sm text-muted",
			children: [
				"开光 ",
				formatNum(power),
				" · 暴机 ",
				(cc * 100).toFixed(0),
				"%"
			]
		}),
		/* @__PURE__ */ jsx("ul", {
			className: "flex flex-col gap-2.5",
			children: CLICK_UPGRADES.map((u) => {
				const lv = clickUp[u.id] ?? 0;
				const maxed = "max" in u && u.max ? lv >= u.max : false;
				const locked = "needSense" in u && u.needSense ? (clickUp.sense ?? 0) < u.needSense : false;
				const cost = clickUpgradeCost(u.id, lv);
				const can = !maxed && !locked && qi >= cost;
				return /* @__PURE__ */ jsxs("li", {
					className: `shop-row flex items-center gap-3 p-3.5 ${locked ? "shop-row-locked opacity-70" : ""}`,
					children: [/* @__PURE__ */ jsxs("div", {
						className: "min-w-0 flex-1",
						children: [
							/* @__PURE__ */ jsxs("div", {
								className: "flex items-baseline justify-between",
								children: [/* @__PURE__ */ jsx("span", {
									className: "shop-row-name text-base font-semibold",
									children: u.name
								}), /* @__PURE__ */ jsxs("span", {
									className: "text-sm text-muted",
									children: ["Lv.", lv]
								})]
							}),
							/* @__PURE__ */ jsx("div", {
								className: "shop-row-sub mt-0.5 text-sm text-faint",
								children: u.desc
							}),
							locked && /* @__PURE__ */ jsx("div", {
								className: "mt-0.5 text-sm text-seal",
								children: "需指尖开光 5 级"
							})
						]
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						disabled: !can,
						onClick: () => useGame.getState().buyClick(u.id),
						className: "buy-btn min-h-11 shrink-0 px-4 text-sm",
						children: maxed ? "已满" : formatNum(cost)
					})]
				}, u.id);
			})
		})
	] });
}
function FatePanel() {
	const jade = useGame((s) => s.jade);
	const disciples = useGame((s) => s.disciples);
	const pity = useGame((s) => s.pity);
	const reveal = useGame((s) => s.gachaReveal);
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "mb-3 flex items-center justify-between",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "panel-title",
				children: "拜山"
			}), /* @__PURE__ */ jsxs("span", {
				className: "text-sm text-muted",
				children: ["缘簿余 ", 40 - pity]
			})]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mb-4 flex gap-2.5",
			children: [/* @__PURE__ */ jsx("button", {
				type: "button",
				disabled: jade < 10,
				onClick: () => useGame.getState().pull(1),
				className: "buy-btn min-h-12 flex-1 text-base",
				children: "投缘 · 10 缘玉"
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				disabled: jade < 90,
				onClick: () => useGame.getState().pull(10),
				className: "buy-btn min-h-12 flex-1 text-base",
				children: "十缘 · 90"
			})]
		}),
		reveal && /* @__PURE__ */ jsxs("div", {
			className: "shop-row mb-4 p-3.5",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "mb-2.5 flex items-center justify-between",
				children: [/* @__PURE__ */ jsx("span", {
					className: "text-sm text-muted",
					children: "此番拜山所得"
				}), /* @__PURE__ */ jsx("button", {
					type: "button",
					className: "min-h-11 px-3 text-sm font-semibold text-seal",
					onClick: () => useGame.getState().clearGacha(),
					children: "收下"
				})]
			}), /* @__PURE__ */ jsx("div", {
				className: "grid grid-cols-5 gap-1.5",
				children: reveal.map((r, i) => {
					const d = DISCIPLES.find((x) => x.id === r.id);
					return /* @__PURE__ */ jsxs("div", {
						className: "flex flex-col items-center",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "flex h-24 w-16 items-end overflow-hidden rounded-xl border border-ink/10 bg-paper-2",
								children: /* @__PURE__ */ jsx(PortraitImg, {
									d: d ?? "outer",
									className: "h-24 w-16 object-cover object-top"
								})
							}),
							/* @__PURE__ */ jsx("span", {
								className: "mt-1 text-[11px]",
								style: { color: inkRarityColor(r.rarity) },
								children: d?.name
							}),
							r.isNew && r.rarity >= 3 ? /* @__PURE__ */ jsxs("span", {
								className: "text-[11px] text-seal",
								children: ["新 · ", d?.title]
							}) : r.isNew ? /* @__PURE__ */ jsx("span", {
								className: "text-[11px] text-muted",
								children: "新"
							}) : null
						]
					}, i);
				})
			})]
		}),
		disciples.length === 0 ? /* @__PURE__ */ jsx("p", {
			className: "text-sm text-muted",
			children: "尚未有人拜山。破层可得仙玉。"
		}) : /* @__PURE__ */ jsx("ul", {
			className: "grid grid-cols-2 gap-2.5",
			children: disciples.slice().sort((a, b) => {
				const da = DISCIPLES.find((d) => d.id === a.id);
				return (DISCIPLES.find((d) => d.id === b.id)?.rarity ?? 0) - (da?.rarity ?? 0);
			}).map((o) => {
				const d = DISCIPLES.find((x) => x.id === o.id);
				if (!d) return null;
				return /* @__PURE__ */ jsxs("li", {
					className: "shop-row flex items-center gap-2.5 p-3",
					children: [/* @__PURE__ */ jsx("div", {
						className: "h-20 w-16 shrink-0 overflow-hidden rounded-xl border border-ink/10 bg-paper",
						children: /* @__PURE__ */ jsx(PortraitImg, {
							d,
							className: "h-20 w-16 object-cover object-top"
						})
					}), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "text-sm font-semibold text-ink",
							children: [d.name, o.stars > 0 ? ` · ${o.stars}印` : ""]
						}), /* @__PURE__ */ jsxs("div", {
							className: "mt-1 flex items-center gap-1.5 text-xs text-muted",
							children: [/* @__PURE__ */ jsx("span", {
								className: "rarity-stamp",
								style: { color: inkRarityColor(d.rarity) },
								children: RARITY[d.rarity]
							}), /* @__PURE__ */ jsxs("span", { children: [d.title, d.rarity >= 3 ? " · 御剑" : ""] })]
						})]
					})]
				}, o.id);
			})
		})
	] });
}
/** 立绘分级：g2=普通档，g3=高稀有档；未列出的沿用原图（从简档） */
var PORTRAIT_GRADE = {
	chiyun: "g2",
	xiaohua: "g2",
	qinghe: "g3",
	jianjiu: "g3",
	xuantian: "g3",
	chuchen: "g3"
};
function portraitSrc(d) {
	if (typeof d === "string") {
		if (d === "mascot") return assetUrl("sprites/mascot.png?v=2");
		return assetUrl(`sprites/disciple-${d}.png?v=2`);
	}
	if (d.portrait) {
		const g = PORTRAIT_GRADE[d.portrait];
		return assetUrl(g ? `sprites/portrait-${d.portrait}_${g}.jpg?v=7` : `sprites/portrait-${d.portrait}.jpg?v=6`);
	}
	if (d.sprite === "mascot") return assetUrl("sprites/mascot.png?v=2");
	return assetUrl(`sprites/disciple-${d.sprite}.png?v=2`);
}
function PortraitImg({ d, className }) {
	const primary = portraitSrc(d);
	const fallback = typeof d === "string" ? assetUrl(d === "mascot" ? "sprites/mascot.png?v=2" : `sprites/disciple-${d}.png?v=2`) : assetUrl(d.sprite === "mascot" ? "sprites/mascot.png?v=2" : `sprites/disciple-${d.sprite}.png?v=2`);
	return /* @__PURE__ */ jsx("img", {
		src: primary,
		alt: "",
		loading: "lazy",
		decoding: "async",
		className,
		onError: (e) => {
			const el = e.currentTarget;
			if (el.dataset.fallback === "1") return;
			el.dataset.fallback = "1";
			el.src = fallback;
		}
	});
}
function RealmPanel() {
	const explores = useGame((s) => s.explores);
	const layer = useGame((s) => s.layer);
	const now = Date.now();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx("h2", {
			className: "panel-title mb-1",
			children: "云游"
		}),
		/* @__PURE__ */ jsx("p", {
			className: "mb-4 text-sm text-muted",
			children: "派山门中人云游闭关。云阶灯火亮起后可多开一路。"
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mb-3 flex flex-col gap-2.5",
			children: explores.map((slot, i) => /* @__PURE__ */ jsxs("div", {
				className: "shop-row p-3.5",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "mb-2 text-sm text-muted",
					children: [
						"第 ",
						i + 1,
						" 路"
					]
				}), !slot ? /* @__PURE__ */ jsx("div", {
					className: "flex flex-wrap gap-1.5",
					children: REALMS.map((r) => {
						const locked = layer < r.unlockLayer;
						return /* @__PURE__ */ jsx("button", {
							type: "button",
							disabled: locked,
							onClick: () => useGame.getState().startExplore(i, r.id),
							className: "buy-btn min-h-11 px-3 text-sm",
							children: locked ? `${r.name} · ${r.unlockLayer}层` : `${r.name} · ${formatTime(r.duration)}`
						}, r.id);
					})
				}) : now >= slot.endAt ? /* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: () => useGame.getState().claimExplore(i),
					className: "buy-btn min-h-12 w-full text-base",
					children: "云游归来"
				}) : /* @__PURE__ */ jsxs("div", {
					className: "text-sm text-ink-2",
					children: [
						REALMS.find((r) => r.id === slot.realmId)?.name,
						" · 剩余 ",
						formatTime((slot.endAt - now) / 1e3)
					]
				})]
			}, i))
		})
	] });
}
function DaoPanel() {
	const dao = useGame((s) => s.daoFruit);
	const skills = useGame((s) => s.skills);
	const lab = useGame((s) => s.labUnlocked);
	const crafts = useGame((s) => s.crafts);
	const alchemy = useGame((s) => s.buildings.alchemy ?? 0);
	const totalDamage = useGame((s) => s.totalDamage);
	const prestigeCount = useGame((s) => s.prestigeCount);
	const fruit = prestigeFruit(totalDamage, multipliers(useGame.getState()).dao);
	const jade = useGame((s) => s.jade);
	const herbs = useGame((s) => s.herbs);
	const ore = useGame((s) => s.ore);
	return /* @__PURE__ */ jsxs("div", {
		className: "flex flex-col gap-7",
		children: [
			/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx("h2", {
					className: "panel-title",
					children: "劫后归山"
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mb-3 mt-1 text-sm text-muted",
					children: [
						"已归山 ",
						prestigeCount,
						" 次 · 本次可得道果 ",
						fruit || "—"
					]
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					disabled: fruit < 1,
					onClick: () => useGame.getState().prestige(),
					className: "buy-btn min-h-13 w-full text-base",
					children: fruit < 1 ? "山息未满，不可归山" : `劫后归山 · 得 ${fruit} 道果`
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-xs text-faint",
					children: "重置山势印记与香火，保留弟子、功法与部分仙玉。"
				})
			] }),
			/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx("div", {
					className: "ink-divider",
					children: /* @__PURE__ */ jsxs("span", { children: ["功法 · 道果 ", formatNum(dao)] })
				}),
				!lab && /* @__PURE__ */ jsx("button", {
					type: "button",
					disabled: jade < 20,
					onClick: () => useGame.getState().unlockLab(),
					className: "buy-btn mb-3 min-h-12 w-full text-sm",
					children: "花费 20 仙玉解锁藏经阁"
				}),
				/* @__PURE__ */ jsx("ul", {
					className: "flex flex-col gap-2.5",
					children: SKILLS.map((sk) => {
						const lv = skills[sk.id] ?? 0;
						const cost = skillCost(sk.id, lv);
						const can = lab && lv < sk.max && dao >= cost;
						return /* @__PURE__ */ jsxs("li", {
							className: "shop-row flex items-center gap-3 p-3.5",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "min-w-0 flex-1",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "flex justify-between text-sm",
									children: [/* @__PURE__ */ jsx("span", {
										className: "shop-row-name font-semibold",
										children: sk.name
									}), /* @__PURE__ */ jsxs("span", {
										className: "text-muted",
										children: [
											lv,
											"/",
											sk.max
										]
									})]
								}), /* @__PURE__ */ jsx("div", {
									className: "shop-row-sub mt-0.5 text-sm text-faint",
									children: sk.desc
								})]
							}), /* @__PURE__ */ jsx("button", {
								type: "button",
								disabled: !can,
								onClick: () => useGame.getState().buySkill(sk.id),
								className: "buy-btn min-h-11 shrink-0 px-4 text-sm",
								children: lv >= sk.max ? "已满" : `${cost} 果`
							})]
						}, sk.id);
					})
				})
			] }),
			/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
				className: "ink-divider",
				children: /* @__PURE__ */ jsx("span", { children: "炼器" })
			}), alchemy < 1 ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-muted",
				children: "建起瀑侧丹灶后方可炼制法宝。"
			}) : /* @__PURE__ */ jsx("ul", {
				className: "flex flex-col gap-2.5",
				children: CRAFTS.map((c) => {
					const lv = crafts[c.id] ?? 0;
					const can = lv < c.max && herbs >= c.herbs && ore >= c.ore && jade >= c.jade;
					return /* @__PURE__ */ jsxs("li", {
						className: "shop-row flex items-center gap-3 p-3.5",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "min-w-0 flex-1",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "shop-row-name text-sm font-semibold",
								children: [
									c.name,
									" · ",
									lv,
									"/",
									c.max
								]
							}), /* @__PURE__ */ jsxs("div", {
								className: "shop-row-sub mt-0.5 text-sm text-faint",
								children: [
									c.desc,
									" · 草",
									c.herbs,
									" 矿",
									c.ore,
									" 玉",
									c.jade
								]
							})]
						}), /* @__PURE__ */ jsx("button", {
							type: "button",
							disabled: !can,
							onClick: () => useGame.getState().craft(c.id),
							className: "buy-btn min-h-11 shrink-0 px-5 text-sm",
							children: "炼"
						})]
					}, c.id);
				})
			})] })
		]
	});
}
function TitleScreen() {
	const go = () => useGame.getState().startGame();
	const taps = useRef([]);
	const onTitleTap = (e) => {
		e.stopPropagation();
		const now = Date.now();
		taps.current = [...taps.current.filter((t) => now - t < 2e3), now];
		if (taps.current.length >= 5) {
			taps.current = [];
			const st = useGame.getState();
			st.setDebug(true);
			st.pushToast("测试模式已开启 · 进山后点右下「试」");
		}
	};
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-auto absolute inset-0 z-50 flex flex-col items-center justify-center bg-gradient-to-t from-ink/60 via-ink/15 to-transparent px-6 pb-[env(safe-area-inset-bottom)] pt-[env(safe-area-inset-top)] text-center",
		onClick: go,
		children: /* @__PURE__ */ jsxs("div", {
			className: "hud-box w-full max-w-sm px-8 py-10",
			children: [
				/* @__PURE__ */ jsx("p", {
					className: "title-line text-sm tracking-[0.55em] text-muted",
					style: { animationDelay: "0ms" },
					children: "天劫未散"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "title-line mt-4 font-display text-6xl tracking-[0.22em] text-ink",
					style: { animationDelay: "80ms" },
					onClick: onTitleTap,
					children: "仙途点修"
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "title-line mt-6 space-y-2 text-lg leading-relaxed text-ink-2",
					style: { animationDelay: "180ms" },
					children: [
						/* @__PURE__ */ jsx("p", { children: "你以凡躯立于劫云之下。" }),
						/* @__PURE__ */ jsx("p", { children: "开光劫云，立门开山，" }),
						/* @__PURE__ */ jsx("p", { children: "拜山投缘，香火伐劫。" })
					]
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: (e) => {
						e.stopPropagation();
						go();
					},
					className: "title-line ink-btn ink-btn-solid mt-8 min-h-14 min-w-48 px-10 text-lg tracking-[0.3em]",
					style: { animationDelay: "320ms" },
					children: "开启仙途"
				})
			]
		})
	});
}
var TUTORIAL = [
	{
		title: "",
		body: "",
		cls: ""
	},
	{
		title: "指尖开光",
		body: "点山中任意处，给空峰开光。连点积「开光连势」。",
		cls: "left-3 top-[24%]"
	},
	{
		title: "落下第一印",
		body: "滑山找到山麓香案虚印，点碑落「开山香案」。",
		cls: "left-[8%] top-[46%]"
	},
	{
		title: "拜山投缘",
		body: "点底栏「拜山」，用缘玉请人上山。",
		cls: "inset-x-4 bottom-36"
	},
	{
		title: "香火自燃",
		body: "云阶亮起后山门会替你开光；金丹以上可御剑绕劫云。",
		cls: "left-3 top-[46%]"
	}
];
function TutorialCoach() {
	const step = useGame((s) => s.tutorialStep);
	const started = useGame((s) => s.started);
	useEffect(() => {
		if (step !== 4) return;
		const t = window.setTimeout(() => useGame.getState().finishTutorial(), 5200);
		return () => window.clearTimeout(t);
	}, [step]);
	if (!started || step < 1 || step > 4) return null;
	const t = TUTORIAL[step];
	return /* @__PURE__ */ jsx("div", {
		className: `pointer-events-none absolute z-30 ${t.cls}`,
		children: /* @__PURE__ */ jsxs("div", {
			className: "coach-card pointer-events-auto max-w-64 px-4 py-3.5",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "font-display text-base font-semibold text-ink",
					children: t.title
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-1.5 text-sm leading-relaxed text-ink-2",
					children: t.body
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-2.5 flex items-center justify-between",
					children: [/* @__PURE__ */ jsxs("span", {
						className: "text-xs tabular-nums text-faint",
						children: [step, " / 4"]
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						className: "min-h-11 px-3 text-sm font-medium text-seal",
						onClick: () => useGame.getState().skipTutorial(),
						children: "跳过"
					})]
				})
			]
		})
	});
}
function MenuSheet() {
	const open = useGame((s) => s.menuOpen);
	const sfxOn = useGame((s) => s.sfxOn);
	const shakeOn = useGame((s) => s.shakeOn);
	const daoFruit = useGame((s) => s.daoFruit);
	const prestigeCount = useGame((s) => s.prestigeCount);
	const wipe = useGame((s) => s.toasts.some((t) => t.text === "再点一次废弃存档"));
	if (!open) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: "absolute inset-0 z-50 flex items-end justify-center bg-ink/35 px-4 pb-[max(112px,env(safe-area-inset-bottom))]",
		children: [/* @__PURE__ */ jsx("button", {
			type: "button",
			className: "absolute inset-0",
			"aria-label": "关闭",
			onClick: () => useGame.getState().setMenu(false)
		}), /* @__PURE__ */ jsxs("div", {
			className: "hud-box relative w-full max-w-sm p-6",
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "mb-4 flex items-center justify-between",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "font-display text-xl",
						children: "宗主"
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						className: "min-h-11 px-3 text-base tracking-widest text-muted",
						onClick: () => useGame.getState().setMenu(false),
						children: "关闭"
					})]
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mb-4 text-sm text-muted",
					children: [
						"已归山 ",
						prestigeCount,
						" 次 · 道果 ",
						formatNum(daoFruit)
					]
				}),
				/* @__PURE__ */ jsxs("label", {
					className: "flex min-h-12 items-center justify-between text-base",
					children: ["音效", /* @__PURE__ */ jsx("input", {
						type: "checkbox",
						checked: sfxOn,
						onChange: (e) => useGame.getState().setSfx(e.target.checked),
						className: "h-6 w-6"
					})]
				}),
				/* @__PURE__ */ jsxs("label", {
					className: "flex min-h-12 items-center justify-between text-base",
					children: ["震屏", /* @__PURE__ */ jsx("input", {
						type: "checkbox",
						checked: shakeOn,
						onChange: (e) => useGame.getState().setShake(e.target.checked),
						className: "h-6 w-6"
					})]
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: () => {
						useGame.getState().setMenu(false);
						useGame.getState().setTab("dao");
					},
					className: "ink-btn mt-3 min-h-12 w-full text-base",
					children: "渡劫与功法"
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					onClick: () => {
						const g = useGame.getState();
						if (wipe) g.resetSave();
						else g.pushToast("再点一次废弃存档");
					},
					className: "ink-btn mt-2.5 min-h-12 w-full text-base text-danger",
					children: "废弃存档"
				})
			]
		})]
	});
}
function Toasts() {
	const toasts = useGame((s) => s.toasts);
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-none absolute inset-x-0 top-[max(64px,env(safe-area-inset-top))] z-40 mx-auto flex max-w-sm flex-col items-center gap-1.5 px-4",
		children: toasts.map((t) => /* @__PURE__ */ jsx("div", {
			className: "toast-pop rounded-xl border border-ink/15 bg-paper/95 px-4 py-2.5 text-sm text-ink shadow-lg",
			children: t.text
		}, t.id))
	});
}
function OfflineModal() {
	const gift = useGame((s) => s.offlineGift);
	if (!gift) return null;
	return /* @__PURE__ */ jsx("div", {
		className: "pointer-events-auto absolute inset-0 z-[60] flex items-center justify-center bg-ink/40 px-6 pb-[env(safe-area-inset-bottom)] pt-[env(safe-area-inset-top)]",
		onPointerUp: () => useGame.getState().dismissOffline(),
		children: /* @__PURE__ */ jsxs("div", {
			className: "hud-box w-full max-w-sm p-8 text-center",
			onPointerUp: (e) => e.stopPropagation(),
			children: [
				/* @__PURE__ */ jsx("h2", {
					className: "font-display text-3xl",
					children: "闭关归来"
				}),
				/* @__PURE__ */ jsxs("p", {
					className: "mt-3 text-base leading-relaxed text-muted",
					children: [
						"离山 ",
						formatTime(gift.sec),
						"，山门代收香火 ",
						formatNum(gift.qi)
					]
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					onPointerUp: () => useGame.getState().dismissOffline(),
					className: "ink-btn ink-btn-solid mt-6 min-h-12 w-full text-base",
					children: "收下"
				})
			]
		})
	});
}
var CHEATS = [
	{
		kind: "qi",
		label: "香火+100亿"
	},
	{
		kind: "jade",
		label: "仙玉+2000"
	},
	{
		kind: "fruit",
		label: "道果+100"
	},
	{
		kind: "buildings",
		label: "全建筑40级"
	},
	{
		kind: "layer10",
		label: "劫层+10"
	},
	{
		kind: "layer50",
		label: "直达50层"
	},
	{
		kind: "unlock",
		label: "系统全开"
	},
	{
		kind: "prestige",
		label: "强行渡劫"
	}
];
function DebugPanel() {
	const debug = useGame((s) => s.debug);
	const [open, setOpen] = useState(false);
	if (!debug) return null;
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx("button", {
		type: "button",
		"aria-label": "测试面板",
		onClick: () => setOpen(true),
		className: "pointer-events-auto absolute bottom-28 right-4 z-40 flex h-12 w-12 items-center justify-center rounded-full border border-seal/50 bg-ink/70 font-display text-xl text-paper shadow-lg",
		children: "试"
	}), open && /* @__PURE__ */ jsx("div", {
		className: "pointer-events-auto absolute inset-0 z-[70] flex items-end justify-center bg-ink/45 px-4 pb-[max(112px,env(safe-area-inset-bottom))]",
		onClick: () => setOpen(false),
		children: /* @__PURE__ */ jsxs("div", {
			className: "hud-box w-full max-w-sm p-6",
			onClick: (e) => e.stopPropagation(),
			children: [
				/* @__PURE__ */ jsxs("div", {
					className: "flex items-center justify-between",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "font-display text-xl",
						children: "测试外挂"
					}), /* @__PURE__ */ jsx("button", {
						type: "button",
						className: "min-h-11 px-3 text-sm text-muted underline",
						onClick: () => {
							useGame.getState().setDebug(false);
							setOpen(false);
						},
						children: "关闭测试模式"
					})]
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-1.5 text-sm text-muted",
					children: "仅测试期使用,不影响正式存档结构"
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-4 grid grid-cols-2 gap-2.5",
					children: CHEATS.map((c) => /* @__PURE__ */ jsx("button", {
						type: "button",
						className: "ink-btn min-h-12 px-3 text-base",
						onClick: () => useGame.getState().cheat(c.kind),
						children: c.label
					}, c.kind))
				}),
				/* @__PURE__ */ jsx("button", {
					type: "button",
					className: "ink-btn ink-btn-solid mt-3.5 min-h-12 w-full text-base",
					onClick: () => setOpen(false),
					children: "收起"
				})
			]
		})
	})] });
}
//#endregion
//#region src/game/GameShell.tsx
var BootError = class extends Component {
	state = { failed: false };
	static getDerivedStateFromError() {
		return { failed: true };
	}
	componentDidCatch(_e, _info) {}
	render() {
		if (this.state.failed) return /* @__PURE__ */ jsx("div", {
			className: "flex h-full items-center justify-center bg-paper px-6 text-center text-ink",
			children: /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
				className: "font-display text-2xl tracking-widest",
				children: "仙途中断"
			}), /* @__PURE__ */ jsx("button", {
				type: "button",
				className: "ink-btn ink-btn-solid mt-5 min-h-12 px-8",
				onClick: () => {
					try {
						localStorage.removeItem("xian-clicker-v1");
						localStorage.removeItem("xian-clicker-v1:bak");
					} catch {}
					window.location.reload();
				},
				children: "重新开启"
			})] })
		});
		return this.props.children;
	}
};
function GameShell() {
	useEffect(() => {
		useGame.getState().hydrate();
		const onFirst = () => sfx.unlock();
		window.addEventListener("pointerdown", onFirst, { once: true });
		const lock = (e) => {
			const t = e.target;
			if (t instanceof Element) {
				if (t.closest(".overflow-y-auto")) return;
				if (!!!t.closest("canvas") && t.closest("[data-allow-touch-scroll]")) return;
			}
			e.preventDefault();
		};
		document.addEventListener("touchmove", lock, { passive: false });
		return () => {
			window.removeEventListener("pointerdown", onFirst);
			document.removeEventListener("touchmove", lock);
		};
	}, []);
	return /* @__PURE__ */ jsx(BootError, { children: /* @__PURE__ */ jsx(Play, {}) });
}
function Play() {
	const started = useGame((s) => s.started);
	return /* @__PURE__ */ jsx("main", {
		className: "absolute inset-0 flex justify-center overflow-hidden bg-void text-ink",
		onContextMenu: (e) => e.preventDefault(),
		onDoubleClick: (e) => e.preventDefault(),
		children: /* @__PURE__ */ jsxs("div", {
			className: "relative h-full w-full max-w-phone overflow-hidden",
			onContextMenu: (e) => e.preventDefault(),
			children: [
				/* @__PURE__ */ jsx(WorldCanvas, {}),
				started && /* @__PURE__ */ jsx(Hud, {}),
				started && /* @__PURE__ */ jsx(TutorialCoach, {}),
				started && /* @__PURE__ */ jsx(Sheet, {}),
				started && /* @__PURE__ */ jsx(BottomNav, {}),
				started && /* @__PURE__ */ jsx(Toasts, {}),
				started && /* @__PURE__ */ jsx(DebugPanel, {}),
				started && /* @__PURE__ */ jsx(OfflineModal, {}),
				started && /* @__PURE__ */ jsx(MenuSheet, {}),
				!started && /* @__PURE__ */ jsx(TitleScreen, {})
			]
		})
	});
}
//#endregion
//#region src/routes/index.tsx?tsr-split=component
function Home() {
	return /* @__PURE__ */ jsx(GameShell, {});
}
//#endregion
export { Home as component };
